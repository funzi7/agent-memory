# paper-trader dashboard — cc-latest handoff

_Generated by a Claude Code session in the firewalled dev sandbox; verified with a
headless Chromium against the live published `portfolios.json`._

## What this change did
Two dashboard DISPLAY fixes (data was already correct). Scope: `funzi7/agent-memory`
only (`paper-trader/app.js`, `paper-trader/sw.js`). SW cache bumped
**pt-dash-v14 → v15**.

### BUG 1 — invested cash shown as a near-total loss
`cashLineHTML` showed the **cash movement** as the since-inception delta:
leveraged_momentum_10k read `מאז התחלה -$9,207.50` because cash went $10,000 →
$792.50 when shares were bought — money that's INVESTED, not lost. Fix: the daily
and since-inception deltas are now the **TOTAL-EQUITY change** (cash + market value
of positions, via `lastEq`/`prevEq`/`account_size`), not the cash balance change.
`מזומן` still shows the real cash balance.
- leveraged_momentum_10k now: `מזומן: $792.50 (יומי -$175.57 · מאז התחלה -$175.57)`
  — consistent with `תשואה מצטברת -1.76%` (was the misleading -$9,207.50).
- leveraged_momentum_100: `$32.30 (… -$0.96)` ↔ -0.96%. Cash-only rsi2/rotation:
  `$10,000.00 (… +$0.00)` ↔ +0.00%. A fully-invested portfolio never reads as a
  total loss. Signed bidi preserved (`<bdi dir="ltr">-$175.57</bdi>`, minus on the
  left). `cashSince`/`cashToday` (cash-movement helpers) are left in place but no
  longer drive the display.

### BUG 2 — "עודכן לפני 2 ימים" for yesterday's date
`relTime` measured hours from **midnight-UTC** of a date-only value, so a ~1.8-day
gap rounded UP to "2 ימים". Fix: for a date-only value (a processed trading DAY) it
now computes the **calendar-day difference** to today (local midnights):
`0 → עודכן היום`, `1 → עודכן אתמול`, `N → עודכן לפני N ימים`. Full timestamps keep
the existing elapsed-time wording. For `last_processed_date 2026-06-24` viewed on
2026-06-25 the header now reads **"עודכן אתמול"** (`נכון לתאריך 2026-06-24` stays).

## Verification (headless Chromium, live data)
- leveraged_momentum_10k: cash **$792.50** (positive), since-inception **-$175.57**
  / -1.76% (total-equity based) — NOT -$9,207.50; no invested portfolio looks like a
  total loss. Header: **"עודכן אתמול"**. Cash-only rsi2: $10,000 / +$0.00 / +0.00%.
  0 JS errors. Signed/`$` runs LTR-isolated (minus on the left).

## Rules honored
Scope `funzi7/agent-memory` only. Returns based on total equity (cash + positions);
`מזומן` shows the real balance; relative-time accurate (yesterday = 1 day). Read-only
page; text follows `paper-trader/RTL-RULEBOOK.md` (`applyRtl()` bidi pass unchanged).
