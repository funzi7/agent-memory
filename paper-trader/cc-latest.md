# paper-trader dashboard — cc-latest handoff

_Generated by a Claude Code session in the firewalled dev sandbox; verified with a
headless Chromium against the live published `portfolios.json`._

## What this change did
Reworded the confusing pace-vs-target line so it can't be misread as a return.
Scope: `funzi7/agent-memory` only (`paper-trader/app.js`, `paper-trader/sw.js`). SW
cache bumped **pt-dash-v16 → v17**.

## The bug
The pace block showed `השגת היעד: 224%` ("target achievement: 224%"), which reads
like a +224% gain — but it actually means "you're at 224% of your self-set MONTHLY
TARGET pace". Real returns are small (e.g. +2.47%, +4.15%), so the big pace number
next to them was misleading.

## The fix (`paceTargetInner`, shared by card + detail page)
Renamed `השגת היעד` → **`קצב מול יעד`** ("pace vs target") and switched to a
MULTIPLE form once past ~150% of pace:
- `ratio >= 1.5` → `×N.N מהיעד` (e.g. `×2.2 מהיעד`, `×18.0 מהיעד`) — clearly a
  multiple of the target pace, not a percent return.
- `ratio < 1.5` → `N% מהיעד` (e.g. `80% מהיעד`).
The pace badge (`🚀 מקדים` / `✅ בקצב` / `⚠️ מאחור`) is kept. The real return stays in
its own separate `תשואה מצטברת` row, so return and pace are now visually + verbally
distinct. The honest user-set-target disclaimer (page footer) is unchanged.
Numbers/`×`/`%` left bare per the RTL rulebook (line is RTL Hebrew).

## Verification (headless Chromium, live data; dashboard + `strategy.html`)
- leveraged_momentum_100: `תשואה מצטברת +2.47%` (return) AND a separate
  `קצב מול יעד: ×18.0 מהיעד 🚀 מקדים` (pace) — the multiple form can't be read as an
  1800% gain. $10k: `+4.15%` return vs `×34.3 מהיעד`. Same on the detail page.
- The old `השגת היעד` wording is gone everywhere; 0 JS errors; bidi correct.

## Rules honored
Scope `funzi7/agent-memory` only. Pace-vs-target reworded so it isn't mistaken for a
return; disclaimer kept; applied on dashboard card + detail page for all portfolios.
Read-only page; text follows `paper-trader/RTL-RULEBOOK.md` (`applyRtl()` unchanged).
