# quality-screener — cc-latest handoff

_Generated by a Claude Code session in the firewalled dev sandbox._

## What this session did
Added two config-driven quality gates, an optional Finnhub data supplement, and
exposed the A/B track + rank in the output files (for the paper-trader dashboard).
Opened **PR #3** to `main` (Codex review) — not a direct push.

PR: https://github.com/funzi7/quality-screener/pull/3

## Changes
- **Finnhub source (supplement, not replacement):** `screener/finnhub_source.py`
  (requests-based, no new hard dep), key-scrubbed, rate-limited (~57/min), cached.
  Per-metric sourcing **Finnhub-first → yfinance → "נתון חסר"** for gross margin
  (`/stock/metric`) and quarterly revenue (`/stock/financials-reported`). No
  `FINNHUB_API_KEY` ⇒ runs fully on yfinance alone. Optional secret wired in
  `screen.yml`.
- **G7 gross-margin floor** (`gates.gross_margin_min`, default 0.40): breach →
  reject; **missing → non-blocking flag** ("נתון חסר"). Flag-only on Track B.
- **G8 growth deceleration** (`gates.growth_deceleration`, soft WARN default):
  recent quarterly YoY < factor×trailing ⇒ flag; no quarterly data ⇒
  "נתון רבעוני לא זמין" and skipped (never guessed). `mode: reject` can block.
- **A/B track + rank in outputs:** `screener_picks.yaml` gains an additive
  `picks:` list (`ticker`, `rank`, `track`, key metrics); `positions:` kept
  verbatim so paper-trader's reader is unchanged. `shortlist.json` gains per-row
  `rank` + a `gates` config block.

## Empirical Finnhub free-tier check — NOT live-verified
`finnhub.io` and Yahoo are **policy-denied (403 on CONNECT)** at the sandbox egress
proxy, so free-tier fields were **not** probed and **no values were fabricated**.
Expectation from Finnhub docs (confirm on a networked machine via
`tools/finnhub_probe.py`): gross margin via `/stock/metric` is likely **free**;
quarterly revenue via the free `/stock/financials-reported` is *as-reported* and may
be **premium-limited / not cleanly extractable** — in which case G8 degrades
gracefully. **Owner action:** run the probe with a key and paste results into
`handoffs/CONTEXT.md`.

## Verification
- `python tests/selftest.py` → **149/149 green** (was 94; +G7, +G8, +enriched
  picks track/rank, +shortlist rank, +offline Finnhub parsing/availability/key-scrub).
- `python main.py --dry-run` → table (new GM% column) + Hebrew report + enriched
  picks YAML; writes nothing. No secrets printed.

## Rules honored
Free data only; fundamentals never fabricated; missing data ⇒ non-blocking
"נתון חסר" (never a guess); both new gates config-driven and reversible.
