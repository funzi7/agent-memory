# OptionsProfitTracker — State

> Living document. Update at the end of every working session.
> Last updated: 2026-05-29 (post commit `973134c`)

## Current focus

Stabilization rounds A → AJ prime are all merged (latest OPT commit `4e221ea`). Pattern to keep: every fix carries a verification log tag that must appear in logcat before it's declared done — "done in code" has bitten us before (CLAUDE.md "already correct" rule).

Next up: **Group S prime** (watchlist add-button + ±3% alerts + auto-update available capital from IBKR sync — see "In-progress feature plan" below). Two device-verification items still pending (pre/post-market price + abnormal-alert percent — see "Awaiting" section). Current backlog + verified-fixed list are below.

## Repo + state

- **Repo:** funzi7/OptionsProfitTracker
- **DB version:** 30 (planned bump to 31 in FIX 1/F for `initial_implied_volatility` column)
- **Branch (current work):** `main`
- **Last commit:** `4303968` (Group AS prime — NEW8 dividend display, 3 fixes in `PortfolioEventsScreen.kt`. AS1: broadened the dividend projection block (~line 641) — outer guard `parsedFuture.size<=2 && parsedHist.size>=2` (was >=3), replaced the monthly-only `avgInterval in 25..35` with classifier `isMonthly=22..38`/`isQuarterly=80..100`, projects when either; each projected entry now estimates `paymentDate = cursor.plusDays(7)` + keeps `dividendAmount=lastAmount`; so NVDA (quarterly, 2 pts) projects its next dividend with amount+pay date instead of yield-only; DIV_PROJECT log added. AS2: dividend list card (~line 844) now shows `$X.XXXX/מניה` (LTR, PrimaryGreen bold) when `event.dividendAmount != null`. AS3: detail dialog (~line 210) shows Israeli dividend tax after gross — `נטו אחרי מס 25%` (×0.75) and `30%` (×0.70) + note "(30% כשיש מינוס במזומן/מרג'ין, 25% כשאין)" using existing totalPerPayment/hasAmounts. liveAmount defaults to event.dividendAmount (line 116) so v8 amount flows through even though v7 fails. NO realizedPnL/DB change. See Group AS prime block below.)
- **Prev commit:** `d025906` (Group AR prime — NEW8 User-Agent on Yahoo dividend fetches. After AQ all 16 tickers fetch and NVDA dividend appears, but BTCI (monthly-distribution ETF) still returned historical=0 from every source. ROOT CAUSE: the Yahoo dividend fetches in `PortfolioEventsScreen.kt` used bare `java.net.URL(url).readText()` with NO User-Agent — Yahoo returns empty/blocked without a browser UA (the working `IvService.fetchYahooPrice` uses HttpURLConnection + `setRequestProperty("User-Agent","Mozilla/5.0")` + 10s timeouts). FIX: added top-level `private fun fetchUrlWithUA(urlStr)` (HttpURLConnection, 10s connect/read, Mozilla/5.0 UA, disconnect in finally) and routed the THREE Yahoo fetches through it — v7 quote detail-dialog (line 132), v7 quote main fetch (line 361), v8 chart dividend-history (line 583). Finnhub (earnings 345, dividend2 395/396, metric 664) and dividend.com left untouched. NO realizedPnL/DB change. See Group AR prime block below.)
- **Prev commit:** `f183e42` (Group AQ prime — NEW8 timing follow-up to AP. AP added holdings to the DIV_CALL ticker list (16 incl. BTCI) but the fetch still processed only the first 9: the fetch `LaunchedEffect` was keyed ONLY on `refreshTrigger` (PortfolioEventsScreen.kt:288), so it ran once at entry when `tickers`=9 (dashboard summary hadn't emitted holdings yet); when tickers grew to 16 the effect didn't re-run, and the 6h cache short-circuited later runs. FIX: `EventsCache.cachedTickerKey: String`; effect key `LaunchedEffect(refreshTrigger, tickers)`; `tickerKey = tickers.map{uppercase}.sorted().joinToString(",")`; cache gate also requires `cachedTickerKey == tickerKey`; store `cachedTickerKey = tickerKey` on fresh fetch; DIV_CALL log of tickerKey vs cachedKey. So when tickers grows to 16 the key mismatches → re-fetch with all tickers. NO realizedPnL/DB change. See Group AQ prime block below.)
- **Prev commit:** `abdb7aa` (Group AP prime — NEW8 dividends/events for stock holdings. ROOT CAUSE: `MainActivity.kt` Screen.PortfolioEvents built `eventTickers` ONLY from open OPTION positions (`openPos.map{it.ticker}.distinct()`), so stock holdings with no open option (e.g. BTCI, monthly dividend payer) were never fetched → zero dividends. FIX (same idea as AC2 social-filter): `val evSummary by dashVm.dashboardSummary.collectAsState(); val eventTickers = (openPos.map{it.ticker} + evSummary.stockPriceByTicker.keys).map{it.uppercase()}.distinct()` + DIV_CALL log. Now the existing reliable Yahoo v7 quote path in PortfolioEventsScreen.kt (~350-372, dividendDate/exDividendDate/dividendRate) runs for holdings too. Dead Finnhub/dividend.com sources left as-is (fail gracefully). NO realizedPnL/DB change. See Group AP prime block below.)
- **Prev commit:** `b641e96` (Group AO prime — revert of AM2's alignment per device feedback: in `PremiumIncomeScreen.kt` day sub-columns both the Hebrew headers ("פרמיות שנאספו:"/"פרמיות שמומשו:") AND the LTR-wrapped English ticker content lines are now `textAlign = TextAlign.Center` (was Right for headers, Left for content). `fillMaxWidth()` + LTR wrap kept; two-column structure unchanged (collected = right weight-1f Box, realized = left weight-1f Box); `סה"כ:` total sub-rows untouched. NO realizedPnL/DB change. See Group AO prime block below.)
- **Prev commit:** `3df0a5d` (Group AN prime — OPTION B for the annual LIVING bar in `ReportGenerator.kt` annual per-month loop (~line 446). AG1 previously SKIPPED any month without a living/growth breakdown, so Jan/Feb/Mar (single combined targets $2000/$2500/$3000, no breakdown) never fed the annual living bar — only Apr/May (~200k THB breakdown) counted. New else branch: when no breakdown but `eff.targetAmount>0`, convert the combined target via `MonthlyTargetCalculator.toUsd(eff.targetAmount, eff.currency.name, ilsRate, thbRate)` (entity's `currency: Currency` enum USD/ILS/THB → `.name` matches toUsd; historical USD months return amount unchanged) and treat it as ALL LIVING — `annualLivingFilledUsd += min(realized,combinedUsd)`, `annualLivingTargetUsd += combinedUsd`, growth gets 0; logs "LIVING-ONLY(combined)". Breakdown-month branch (Apr/May growth spill) + annual caps unchanged. NO realizedPnL/DB change. See Group AN prime block below.)
- **Prev commit:** `038e43c` (Group AM prime — AM1 monthly target card: status moved INSIDE the progress bar (single full-width 20.dp Box, LTR; left-grown fill with tiered colors, centered white %, "נשאר $X"/"✓ הושג היעד" anchored CenterEnd inside the bar — replaced the old separate right-side label Row); AM2 PremiumIncomeScreen journal sub-columns: Hebrew headers now `fillMaxWidth()+TextAlign.Right`, English ticker content lines `fillMaxWidth()+TextAlign.Left`; AM3 added full-width HorizontalDivider between days inside a week (end of `week.days.forEach`); AM4 WatchlistScreen now shows per-ticker day change % (LTR, sign left, pnlColor) + abnormal flag (bold + ⚠) using `WatchPriceInfo` holder, threshold = 2× `IvService.fetchAvgDailyMovePct` clamped [3,25] fallback 5%, WATCHLIST_VOL log. No realizedPnL/DB change. See Group AM prime block below.)
- **Prev commit:** `973134c` (Group AL prime — DashboardScreen.kt: CC reminder header was bidi-SCRAMBLED on device ("תזכורת 3 - Covered Call טיקרים זמינים") because one Text mixed Hebrew+English+digit. Split into single-token Text pieces in a natural-RTL Row (children place right→left), LTR-wrapping only "Covered Call" + the count so they stay literal; reads correctly "💡 תזכורת Covered Call - 4 טיקרים זמינים". Same pattern as AC1 days-grammar fix. See Group AL prime block below.)
- **Prev commit:** `43561c9` (Group AK prime — DashboardScreen.kt: CC reminder header separator `·`→`" - "`; annual progress card now mirrors the monthly bar — PercentPill+Spacer removed, progressColor switched to the monthly tier palette (2E7D32/689F38/F9A825/EF6C00/C62828), centered LTR white % overlay re-added inside the 22.dp bar track. SegmentedTargetBarSummed untouched. See Group AK prime block below.)
- **Prev commit:** `4e221ea` (Group AJ prime — NEW1 the position-edited feed entry now shows WHAT changed instead of a static "Strike X • N חוזים". In `AddPositionViewModel.kt` after `updatePosition(finalMerged)`, capture `oldPos = existing` before the feed block shadows `existing`, then diff old vs new for strike/contracts/premium/expiration/IV (doubles via `dNeq` tolerance 0.0001) building a `changes` list of "old ← new" tokens (RTL right→left reads from→to), joined with " · " as the description; static text kept as fallback when nothing changed. ACTIVITY_FEED log added. See Group AJ prime block below.)
- **Prev commit:** `0755dd3` (Group AI prime — NEW27 per-ticker volatility-based abnormal-move alert threshold = 2× the ticker's avg daily move clamped [3,25]%, fallback flat 5%; avg daily move cached once/day/ticker in the snapshot JSON as `avgDailyMovePct`+`volComputedDate` — NO DB schema change. See Group AI prime block below.)
- **Prev commit:** `dbdb9bf` (Group AH prime — refined the AG2 total progress bar: bar FILL now uses the same tiered PercentPill colors (≥100% dark green 2E7D32, ≥75% olive 689F38, ≥50% amber F9A825, ≥25% orange EF6C00, else red C62828) instead of flat AccentBlue; "נשאר $X"/"✓ הושג היעד" status label moved to the RIGHT (first child in the RTL Row); track Box wrapped LTR so the fill grows from the LEFT toward the label. מחיה/גדילה bars untouched)
- **Recent commits:** `8a44bd4` → `9ddca1c` (Group A) → `f1c3e9a` (Group B) → `16fd852` (Group C) → `fdc1cf1` (Group D prime) → `7df9465` (Group E prime) → `589b44e` (Group F prime) → `646a1e0` (Group G prime) → `be9a23e` (Group H prime) → `996ffe6` (Group I prime) → `9f79c9f` (Group J prime) → `75722b0` (Group K prime) → `6684fa4` (L' step 1 diag) → `3da5058` (Group L prime) → `91fa735` (Group M prime) → `8bd1aea` (Group N prime) → `3e477be` (Group P prime) → `d8ba47f` (Group Q prime) → `ac6bbd6` (Group R prime) → `ec0a56e` (Group R2 prime) → `8734bdb` (Group S0 prime) → `752b8cb` (Group T prime) → `550dbaa` (Group U prime) → `bfe3f37` (Group V prime) → `65252dc` (Group W prime) → `635543a` (Group X prime) → `2a49f28` (Group Y prime) → `cecb3e2` (Group Z prime) → `4eb0f9f` (Group AA prime) → `54c1730` (Group AB prime) → `ba13041` (Group AC prime) → `f8c9770` (Group AD prime) → `9e1b2d2` (Group AE prime) → `3b0aea4` (Group AF prime) → `d3ddc3b` (Group AG prime) → `dbdb9bf` (Group AH prime) → `0755dd3` (Group AI prime) → `4e221ea` (Group AJ prime) → `43561c9` (Group AK prime) → `973134c` (Group AL prime) → `038e43c` (Group AM prime) → `3df0a5d` (Group AN prime) → `b641e96` (Group AO prime) → `abdb7aa` (Group AP prime) → `f183e42` (Group AQ prime) → `d025906` (Group AR prime) → `4303968` (Group AS prime)
- **Agent-memory last commit:** `ad5cf15`+ (this state.md repo, funzi7/agent-memory)

## Active issues

### Group A (FIX 4) - COMPLETE
A1 strategy sort, A2/A3 source column click+shorten, A4/A5 image cache+URL normalize: all verified. A6 abnormal alerts: partial - only one ticker shows when multiple should (still open, see N-items).

### Group B (FIX 3) - COMPLETE
B1 duplicate removed, B2 yearly 24 percent removed, B4 remaining overlay, B5 monthly target unified 5066: all verified. B3 skipped (minus already correct).

### Group C (FIX 2) - COMPLETE
C2 INTC aggregation verified (2026-only correct), C3 settings target reload verified, C4 post paragraphs verified. C1 CSP cash: superseded by E prime and F-area work.

### Group D prime - COMPLETE
D1 social regression fixed (section restored), D3 reports all-years toggle verified, D4 per-bar remaining done. D2 CSP text: superseded by E prime.

### Group E prime - COMPLETE
E1 CSP card: found root cause - premium is per-share not per-contract. ASTS net = (150 - 9.31) x 100 x 1 = 14069 not 14991. Number now correct. Card restructured RTL with 3 rows (exit cash, available now, after assignment).
E2 monthly bar: realized/target/remaining on each bar, total row RTL.
E3 phantom IREN: added AlertSource enum + badges (bait/watchlist/draft/other). Root cause NOT fixed - just labeled. Superseded by F4.
E4 feed dedup: edit-then-close now updates same row instead of stacking.

### Group F prime - COMPLETE (commit 589b44e)
F1 assignment prob: added DELTA_DEBUG log, made BlackScholesCalculator unit-tolerant (accepts IV as percent or decimal via <5.0 heuristic). Awaiting device verification of EWY showing ~95 percent.
F2 remove IV autofill: stripped all 8 lookupIvForExpiry call sites, removed init cachedIv overwrite. IV now manual-only, user-typed value survives field changes.
F3 feed color: POSITION_EDITED moved to blue (PremiumReceivedColor) branch.
F4 phantom prune: added isFullSync param, prune runs only on fullResync, removes snapshot tickers absent from Flex with no open position and no manual override.

### Group G prime - COMPLETE (commit 646a1e0)
G1 assignment prob IV-free fallback: added assignmentProbability field, BS-delta when IV present else logistic moneyness fallback, hides row when null/zero (no more "very low 0 percent"). Awaiting device verify of RIOT/PLUG now showing probability.
G2 stale prices off-hours: triggerPriceRefresh now writes fetched Yahoo prices back to currentStockPrice (the field ReportGenerator reads), not just the snapshot. Awaiting device verify.
G3 monthly progress percent restored as single LTR row.
G4 required-collateral label now PUT-only (CC no longer shows it).

### Group H prime - COMPLETE (commit be9a23e)
H1 monthly bar reverted: removed the G3 row addition below the bar. Replaced with a PercentPill above the bar matching the AnnualTargetCard pattern (single source of truth). Negative percent clamped to 0 for the pill so a net-loss month doesn't render as nonsense. Added MONTHLY_PCT log to diagnose any sign issue from `realizedForProgress / monthlyTarget`.
H2 BS fill cache-IV fallback: `fillPremiumFromBS` now reads `ivCacheDao.getLatestIvForTicker(ticker)` when the IV field is empty, sets it into state, and re-runs `recalculate()`. If still no BS price, surface a Hebrew autoFillStatus message ("הזן IV..." or "לא ניתן לחשב...") instead of silently writing 0.00. Added BS_FILL log inside `BlackScholesCalculator.calculate` to confirm inputs.
H3 stale price baseline: removed the `else if (oldCurrent != null && oldCurrent != yahooCurrent) obj.put("previous", oldCurrent)` fallback from BOTH price-refresh paths in DashboardViewModel. The fallback was mutating "previous" forward over refreshes, inflating daily change (ASTS real +6% → app +13%). Also flipped abnormal-alert base order to `previous ?: dailyOpen` (was `dailyOpen ?: previous`) — dailyOpen is captured opportunistically and drifts.

### Group I prime - COMPLETE (commit 996ffe6)
I1 monthly bars fallback: rewrote the `else` branch of `MonthlyTargetCard` (when `living > 0 && growth > 0` fails). Now always renders a single 22.dp bar with a centered LTR overlay showing `realized / target · נותר remaining` (or `✓ הושג realized / target` when met). Previously it showed only "נותר: $X" centered on a thin 5%-wide bar, which the user perceived as "no bar". Added MONTHLY_BARS diagnostic log in ReportGenerator that prints `target=<bool> living=<n> growth=<n> overallTarget=<n>` so we can tell if the breakdown is missing because the user never saved one (most likely) vs. a data-load failure.
I2 BS fill IV-parse hardening: in BOTH `recalculate()` AND `fillPremiumFromBS()` the IV is now parsed via `.trim().removeSuffix("%").trim().toDoubleOrNull()` so "45%" / " 45.0 " / "45.0  " all yield 45.0 (previously they parsed to null and BS computed nothing). Comprehensive BS_FILL diagnostic log added showing every parsed input (S, K, dte, ivField, ivParsed, type, bsTheoretical). The "near-zero" guard widened from `<= 0` to `< 0.005` so a positive-but-rounds-to-0.00 BS result triggers the Hebrew explanation branch instead of silently writing "0.00" into the premium field. Added a dedicated "פרמיה תיאורטית קרובה ל-0 — האופציה מאוד OTM" message for that case.
I3 stale price baseline (continuation): H' fixed the `triggerPriceRefresh` path but missed `refreshOnResume` (line 607) and `pullToRefresh` (line 660) — those still had `else if (oldCurrent != null && oldCurrent != yahooCurrent) obj.put("previous", oldCurrent)` which mutated "previous" forward on every resume / pull. Removed in both paths; only Yahoo's regularMarketPreviousClose can update "previous". Added PRICE_REFRESH logs in both paths showing `oldCurrent → newCurrent` and `oldPrev / yahooPrev` so the next device test can confirm fresh baselines.

### Group J prime - COMPLETE (commit 9f79c9f)
J1 stop zeroing living/growth + carry-forward repair: MONTHLY_BARS log on commit 996ffe6 proved `target=true living=0.0 growth=0.0 overallTarget=5073.0` — a Flex sync was overwriting the user's living/growth with zeros. Root cause was `ImportViewModel.applyPortfolioToSettings` constructing a `MonthlyTargetEntity` without the breakdown fields, defaulting them to 0.0. Fix:
  - `applyPortfolioToSettings`: now calls `resolveBreakdown(existing)` which preserves the existing record's living/growth, OR carries forward from the most recent month that has a non-zero breakdown.
  - `FlexSyncWorker.upsertMonthlyFinancialMetrics`: when creating a new-month target, also copies forward prior month's living/growth (so month rollovers don't wipe).
  - `ReportGenerator` (read-time repair): when the current-month target has zero breakdown but a prior month has one, in-memory borrows that breakdown so the two-bar `SegmentedTargetBar` renders again immediately — no need to wait for a Flex sync to persist the repair.
  - New repository helper `getLatestTargetWithBreakdown(excludeYearMonth)` (and corresponding `MonthlyTargetDao.getAllOnce()` filter in Kotlin) finds the latest month with non-zero breakdown excluding a given month.
  - SettingsScreen's existing `getLatestTarget()` fallback at load is already correct — its `defaultTarget` creation only fires when no target exists anywhere, so leaving breakdown=0 there is fine.
J2 price refresh fires for open-position tickers: PRICE_REFRESH log was empty because `refreshOnResume` and `pullToRefresh` only iterated `snapshot.keys()`, and ASTS (a pure-CSP underlying with no shares) never lived in the stock snapshot. Both paths now build `tickers = snapshotTickers ∪ openPositionTickers` (matching what `triggerPriceRefresh` already did from G'), CREATE a fresh snapshot entry for any open-position ticker not yet present, and persist the new price to `position.currentStockPrice` so ReportGenerator's per-position daily-change calculations see fresh values. Added unconditional `refreshOnResume ENTERED` / `triggerPriceRefresh ENTERED` logs at the top of each function so an empty log unambiguously means lifecycle wiring is broken (rather than empty-ticker early-return).

### Group K prime - COMPLETE (commit 75722b0)
J' got the refresh firing and covering open-position tickers (PRICE_REFRESH log now shows ASTS being fetched) but device test revealed two remaining issues — ASTS still displayed +13% (yesterday's close-to-close) instead of today's pre-market ~+6%, and the write-back log said "Wrote currentStockPrice to 0 open positions".
K1 pre/post-market-aware batch fetch: the v7 `/finance/quote` endpoint that `fetchYahooPrices` uses does NOT include pre-market or post-market prices — during overnight sessions it just returns yesterday's regular close as `regularMarketPrice`, so "current" never moved off 119.7 for ASTS. Added `fetchYahooPricesWithPrePost(tickers)` to IvService that loops the existing `fetchYahooPrice(ticker)` (v8 `/finance/chart?includePrePost=true`) per ticker; this endpoint properly surfaces `preMarketPrice` / `postMarketPrice` and `previousClose`. All three dashboard refresh paths (`triggerPriceRefresh`, `refreshOnResume`, `pullToRefresh`) now use it. Also pointed the shared `fetchPricesWithFallback` at the new pre/post variant so any caller using the fallback chain gets pre-market too. (Other callers of `fetchYahooPrices` outside the dashboard refresh — workers, alerts — left alone.)
K2 removed same-price write-back skip: the `if (pos.currentStockPrice != null && kotlin.math.abs(pos.currentStockPrice - fresh) < 0.0001) continue` guard appeared in all three refresh paths. When the v7 endpoint returned yesterday's close (== whatever was already stored), every position got skipped and `positionsUpdated=0`. Removed in all three paths; idempotent writes are fine and `updatedAt` should refresh so downstream Flow listeners react. Added a per-ticker `DAILY-CHK` log printing `current / prev / pct` so the device test can confirm pre-market values without parsing the larger PRICE_REFRESH stream. PositionEntity has no `previousStockPrice` column, so the daily-change baseline lives in the snapshot's `"previous"` key — already getting written from Yahoo's `regularMarketPreviousClose`, fixed back in I'. No DB schema changes here.

### Group L prime - COMPLETE (commits 6684fa4 diag + 3da5058 fix)
K' added the `fetchYahooPricesWithPrePost` path but ASTS still showed +13% from yesterday — turned out K' was just routing through a v8 endpoint that ALSO didn't surface the pre/post data the way we assumed.
Step 1 (diagnostic, commit 6684fa4): added a `PREPOST_DEBUG` log inside `fetchYahooPrice` dumping every raw `meta` field plus the full key list, and confirmation logs at all four dashboard fetch call sites. The PREPOST_DEBUG output from the device proved that on the v8 `chart` endpoint `meta.preMarketPrice` and `meta.postMarketPrice` simply don't exist (they came back NaN, and the meta key list never contained them). The pre/post field naming we assumed only applies to the v7 `quote` endpoint — and that one doesn't include pre/post at all. So neither endpoint, as we were calling them, returned an extended-hours price.
Step 2 (fix, commit 3da5058): on the v8 chart endpoint the latest extended-hours trade actually lives in the per-minute candle arrays at `result[0].indicators.quote[0].close` (with matching `result[0].timestamp`). Now `fetchYahooPrice` walks the close array backwards, picks the most recent non-null close, compares its timestamp against `meta.regularMarketTime`, and if the candle is newer it returns the candle's close as "current" (otherwise falls back to `regularMarketPrice`). `previousClose` comes from meta as before. URL already had `interval=1m&range=1d&includePrePost=true` so the candle array does include pre/post prints. Function signature `(ticker: String): Map<String, Double>` unchanged so all callers (including the K' batch wrapper) just work.
The L1 / CANDLES / FINAL PREPOST_DEBUG logs are left in place so the next device test can verify the new logic produces sensible values.
v7 batch `fetchYahooPrices` (used by workers, alerts, anything outside the dashboard refresh) left alone — only the v8 single-ticker path changed.

### Group M prime - COMPLETE (commit 91fa735)
Device test with markets OPEN: top gainers/losers + abnormal alerts still showed ASTS at +20% when reality was +6%. Current price was correct (127.4 fresh from L' candle-array fix), but the snapshot's "previous" baseline was 105.86 — a value from 2 days ago, not yesterday's close of 119.7.
M1 stop the current→previous demote in Flex/import paths: the dashboard refresh paths (Yahoo) had been writing the correct previous from `regularMarketPreviousClose` since I'/J'/K'. The corruption was coming from THREE non-dashboard sites that did `existing.put("previous", lastCurrent)` on every Flex/sync run — exactly the demote anti-pattern fixed in H' for the dashboard. Locations: `FlexSyncWorker.kt:259-264` (background Flex sync), `ImportViewModel.kt:617-624` (mark-price sync for open positions), and `ImportViewModel.kt:659-666` (stock-holding merge loop). All three now write `current` only; `previous` is owned by Yahoo refresh paths. Added a `snapshot write` PRICE_REFRESH log in the init refresh path showing the written current/previous/pct so the next device test can verify.
M2 IV_TRACE diagnostic (no calc change): once on commit 3da5058 device test, an open CSP position showed delta=-0.999 (vs broker -0.191) and BS=$62.51 (vs broker mid $3.24), self-corrected after the user re-entered IV minutes later. Added `IV_TRACE` log inside `ReportGenerator.summarize` printing `ticker / optionType / K / S / storedIv / dteUsed / delta / prob` at the exact moment estimatedDelta + assignmentProbability are computed. No calc changes — purely diagnostic so next recurrence reveals whether `entity.impliedVolatility` momentarily held the wrong value (stale sync IV, race between user input and persist, etc.).
M3 expected-move range in add-position risk panel: next to the existing "תנועה צפויה (1σ) ±$X" row, added a "טווח צפוי" row showing `$low – $high` (currentPrice ± expectedMove), both wrapped in LTR per CLAUDE.md money-sign rule. Renders only when currentPrice > 0 && expectedMove > 0 so the panel doesn't show a zero-zero range on bare/blank forms.

### Group N prime - COMPLETE (commit 8bd1aea)
N'1 PercentPill realized source aligned with bars: the pill showed 0% while the bars were full and the total row said "✓ הושג היעד". Root cause: `progressPercent` in `ReportGenerator` was computed from `realizedForProgress = ibkrTotalRealized ?: totalRealized` (= 0 for this user — they had no IBKR-tagged closes this month so `ibkrTotalRealized` resolved to a non-null but 0-summed value), while the bars use `combinedTotalRealized` (= ibkrPnl ?: calc per-position, which sums to the real number). Switched `progressPercent` to use `combinedTotalRealized / monthlyTarget * 100`, same value the bars and `totalRealizedProfitThisMonth` flow from. Single source of truth restored. Added `MONTHLY_PCT` log printing both values for verification.
N'2 RTL collateral labels (N7): in `DashboardScreen.kt:2249` the open-position card's "בטחונות: $X" / "מניות: $X" was wrapped in `LayoutDirection.Ltr`, which forced left-to-right reading order and put the Hebrew label visually before the number. Replaced with an RTL Row containing a Hebrew label Text followed by an LTR-wrapped number Text — now label sits on the visual right with the number to its left, matching the rest of the Hebrew card.
N'3 removed debug notifications (N14): three debug surfaces were leaking into the system tray / on-screen — `MainActivity.kt:69-73` Toast "MainActivity.onCreate fired", `OptionsTrackerApp.kt:40-46` Toast "App.onCreate fired @ ...", and `AlertWorker.getForegroundInfo()` notification "בודק התראות..." on the HIGH-importance ALERT channel. Removed both Toasts; kept the logcat MAIN_ONCREATE / APP_ONCREATE entries for diagnostics. AlertWorker's foreground-service notification (Android-required for foreground workers) moved to a new `SILENT_WORKER_CHANNEL_ID` channel (IMPORTANCE_MIN, no sound/vibration/lights), title stripped, `setSilent(true)` + `PRIORITY_MIN`. Real user features (MonthlyReminderWorker reminder, NotificationHelper.showAlertNotification price alerts) left as-is.
N'4 hide IV-opportunity hint on edit (N11): in `AddPositionScreen.kt:458-469` the "🔥 IV גבוה — הזדמנות למכירה" / "📈 IV מעל ממוצע. פרמיות סבירות" / etc. recommendation was rendering on edit-of-existing-open-position too, where the "sell now" framing made no sense (the trade is already open). Wrapped the recommendation block in `if (state.editingPositionId == null || state.isEditingDraft)` so it only shows when adding a new position or editing a draft. The surrounding IV/price/earnings info row is left visible in all modes — that data is useful regardless.

### Group P prime - COMPLETE (commit 3e477be)
P1 EditAssignmentCard RTL: in the CSP "אם תוקצה" panel, the title / subtitle / "מחיר רכישה" rows were correctly right-aligned (inherited the global RTL), but the middle rows ("ייצא מהקאש" / "יתרה זמינה כעת" / "יתרה לאחר הקצאה") sat on the visual left. Root cause: those rows were wrapped in an EXTRA `CompositionLocalProvider(LocalLayoutDirection provides LayoutDirection.Rtl)` + `Alignment.End` / `Arrangement.End`. Inside an already-RTL screen, `End` resolves to the visual LEFT, so the wrapper effectively double-inverted them. Removed the extra wrapper, flipped `End` → `Start` on the Column's `horizontalAlignment` and the rows' `horizontalArrangement` (in global RTL, `Start` = visual right). Inner LTR providers around each `$X,XXX` amount are preserved so the digits keep reading left-to-right.
P2 ticker leak in edit screen (RKLB showing MU): root cause was that `loadPosition()` updates `state.ticker` and a handful of position-specific fields but did NOT clear the ticker-specific cached analyses (`tickerAnalysis`, `strategicAnalysis`, `chainAnalysis`, `wheelSummary`, `lastHistoricalPremium`, `tickerHistory`, `autoFillStatus`, `showChainResult`). Those linger from whatever ticker the ViewModel was last viewing — so opening RKLB edit while the VM still held an MU analysis from a previous session surfaced Micron text inside the RKLB screen. `updateTicker(value)` already nulls `tickerAnalysis` on user-typed input; `loadPosition` now does the same for the full bundle of cached fields. Added `EDIT_LEAK` log confirming the clear.
P3 social feed chronological + timestamps (N13): three coordinated changes — (a) added `publishedAtEpochSec: Long?` to `TelegramPost` and `SocialFeedPost`; (b) `fetchTelegramPostsWithImages` now extracts `<time datetime="ISO8601">` inside each `tgme_widget_message_wrap` block via a `Regex("""<time[^>]*datetime="([^"]+)"""")` and parses to epoch seconds via `OffsetDateTime.parse(it).toEpochSecond()`; (c) `DashboardViewModel` now sorts `allPosts.sortedByDescending { publishedAtEpochSec ?: 0L }` before the filtering/distinctBy/take chain (those are order-preserving), and downstream references switch from `allPosts` to the new `sortedPosts`. Twitter/Nitter posts keep `publishedAtEpochSec = null` (no time tag extracted from Nitter HTML yet) so they fall to the end. UI: added a small LTR `dd/MM HH:mm` stamp next to the `📨 ${channel}` header in all three render sites (compact list, uniform grid card, expanded dialog) so the user immediately sees which channels post freshest.

### Group Q prime - COMPLETE (commit d8ba47f)
Q1 "% על ההון" wrong realized source: the dashboard "% על ההון" card (`DashboardScreen.kt` Row 4 → `summary.profitPercentOnAllocated`) always showed -0.80%. Same root as N'1 — `ReportGenerator.summarize` computed `profitPercentOnPortfolio` / `profitPercentOnAllocated` from `realizedOnly = ibkrTotalRealized ?: totalRealized` (the broken ~-435 IBKR-only sum), NOT the `combinedTotalRealized` (~5286, `ibkrPnl ?: calc` per-position) that the bars / total row / N'-fixed `progressPercent` all consume. -435 / ~54k ≈ -0.80%. Switched both percent fields to `combinedTotalRealized / portfolioForPercent * 100`. Added `EQUITY_PCT` log. NOTE: ReportsScreen's "% פורטפוליו"/"% על ההון" come from the SEPARATE `getMonthlyReport` function via `report.portfolioReturn`/`report.allocatedReturn`, which already use `realizedProfit = ibkrRealizedPnl ?: realizedPnL` (the combined formula) — so that path was already correct and left alone. The device bug was the dashboard card (function A), not the reports card (function B).
Q2 removed duplicate dashboard target banner: a standalone "Month status badge" item, positioned between the social feed and the pre-market/large-move alerts, rendered `summary.monthVsTargetStatus` ("🎯 יעד הושג!" when progress ≥100) — duplicating the MonthlyTargetCard's own "✓ הושג היעד". Removed the entire `if (summary.monthVsTargetStatus.isNotEmpty()) { item {...} }` block. The MonthlyTargetCard / SegmentedTargetBar internal "✓ הושג היעד" text was NOT touched.
Q3 social toggle moved onto header row: the "רק טיקרים בתיק" Switch was its own `item {}` row below the "📱 רשתות חברתיות" header. Merged both into one `item {}` with a `SpaceBetween` Row (global RTL → Hebrew header on the visual right, toggle on the visual left). The "מוצגים X מתוך Y" filtered-count item between them is preserved. (Edited via PowerShell line-reconstruction because the header line stores the emoji as a literal `📱` escape that the Edit tool's JSON layer kept converting to the emoji char — reused the original emoji/Hebrew lines verbatim rather than retyping.)
Q4 finished clearing edit-screen leak: P2 cleared tickerAnalysis/strategicAnalysis/chainAnalysis/wheelSummary/autoFillStatus/lastHistoricalPremium/tickerHistory in `loadPosition`, but a generic "defense companies" headline still leaked into RKLB edit — it lived in the uncleared `aiAnalysis` text field (RKLB is a space/defense underlying, so a stale AI/sector analysis persisted). Added `aiAnalysis`, `aiAnalysisError`, `aiAnalysisLoading`, `chainParsingText`, `showManualChainEntry`, `manualChainRows`, `chainBidAskOverrides` to the loadPosition clearing block. Updated the `EDIT_LEAK` log to list every field cleared. No realizedPnL / DB schema changes.

### Group R prime - COMPLETE (commit ac6bbd6)
All six fixes in `PremiumIncomeScreen.kt` (the income journal screen) — no realizedPnL / DB changes.
R1 rename "מפגרים" → "מפסידים": section header "מובילים ומפגרים לפי P&L" and the loss-card title "מפגרים" both now say "מפסידים" (losers).
R2 center "ניצחונות"/"הפסדים" StatMini cards: the shared `StatMini` Column now has `Modifier.fillMaxWidth()` and both the value and label Texts use `Modifier.fillMaxWidth()` + `textAlign = TextAlign.Center`, so content centers inside the card instead of hugging the RTL start edge.
R3 weekly-summary RTL: the week-header "ממומש: $X" was one combined string inside an LTR span (bidi put the Hebrew label on the LEFT). Split into a Row { Hebrew label "ממומש:" (inherits RTL → right) + Spacer + LTR-wrapped number }. Same split applied to the per-day "סה\"כ:" totals in both columns (R4 region). The week-total row ("סה\"כ שבוע" + number) was already label-right/number-left and left as-is.
R4 hide empty day sub-sections: the two-column day detail used to always render the "פרמיות שנאספו:" / "ממומש:" labels with a "—" placeholder when empty. Now computes `showPremiumCol = opened.isNotEmpty() || dayPremium > 0.0` and `showRealizedCol = closed.isNotEmpty() || dayRealized != 0.0`; each `Column` (and the middle `Spacer`) is wrapped in `if (...)` so an empty side is fully absent (no label, no dash). Removed both `Text("—")` placeholders.
R5 align day-table columns: the header Row uses weights יום=0.8 / תאריך=1 / פרמיות=1.2 / ממומש=1.2 (total 4.2) but the data row used 0.8/1/0.4 (a single bullet dot, total 2.2) so nothing lined up. Replaced the dot with two LTR amount columns at weight 1.2 each: premium = `day.premiumReceived - day.premiumPaid` (PremiumReceivedColor), realized = `day.realizedOnDay` (pnlColor), both `formatCurrency`, `textAlign = End`, empty string when 0. Data row now 0.8/1/1.2/1.2 matching the header.
R6 contain "רווח לפי חודש" month bar: the per-month bar `Box(weight(1f))` sat directly adjacent to the amount (no gap) so the bar visually ran into the number. Added `Spacer(Modifier.width(8.dp))` between the weighted bar Box and the LTR amount, and changed the amount from `Modifier.width(72.dp)` to `Modifier.widthIn(min = 64.dp)` so long values aren't clipped. The bar lives inside its weighted Box and can no longer overlap the number's area.
NOTE on editing this file: it stores `●` (bullet) and `‎` (LTR mark) as LITERAL backslash-escapes and uses `\"` inside Hebrew strings ("סה\"כ:"). The Edit tool's JSON layer mangles `\uXXXX`/`\"`, so the data-row + day-detail rewrite (R3/R4/R5) was done via PowerShell line-splice with single-quoted here-strings (which keep Hebrew, `"`, `\`, `$` literal). Simple Hebrew-only edits (R1, R2, R3 header, R6) went through the Edit tool fine.

### Group R2 prime - COMPLETE (commit ec0a56e)
Follow-up to R5: the two weekly day-table amount columns were ambiguous — a per-day number (e.g. $331.19) looked like it floated between the "פרמיות"/"ממומש" headers. Root cause was a header/data mismatch: each amount cell is LTR-wrapped with `textAlign.End` (number hugs the right edge of its slot) while the RTL headers used `textAlign.End` too (label hugs the left edge of the same slot), so label and number sat at opposite ends of the column — and identical-but-tight 1.2/1.2 weights gave no clear separation or color cue. All in `PremiumIncomeScreen.kt` WeekCard. No realizedPnL / DB changes.
R2'1 column clarity: renamed + recolored the two headers to one-word, color-coded labels — "נאספו" (collected, `PremiumReceivedColor` blue) and "מומשו" (realized, `PrimaryGreen`). Header + data row now use IDENTICAL order and weights (יום 0.7 / תאריך 0.9 / נאספו 1.3 / מומשו 1.3), both inherit global RTL, `maxLines = 1` on the amount cells, numbers still LTR-wrapped. The color match (blue header over blue number, green header over green/red number) is the real disambiguator. Also renamed the day sub-detail left header "ממומש:" → "פרמיות שמומשו:" and recolored it `PrimaryGreen` (the right sub-header "פרמיות שנאספו:" stays blue).
R2'2 week totals split: the week header was restructured from a `Row(SpaceBetween){ title; Column(End){totals} }` into a `Column(clickable){ title Row; totals Row }`. The totals Row is full-width `SpaceBetween` → "סה\"כ נאספו: $X" collected on the visual RIGHT (blue, first child in RTL), "סה\"כ מומשו: $Y" realized on the visual LEFT (green/red). Hebrew label + LTR-wrapped number in each. The bottom "סה\"כ שבוע" footer row was left untouched (out of scope).
NOTE: R2'2 was written via PowerShell line-splice because the new strings contain `\"` (סה\"כ) which the Edit tool's JSON layer mangles; the Hebrew-only / ASCII R2'1 edits used the Edit tool fine.

### Group S0 prime - COMPLETE (commit 8734bdb)
S0'1 pre/post-market baseline corrected (THE root cause of the long-running ASTS % mismatch): device logs at pre-market open PROVED it — `CANDLES ASTS lastClose=125.2 regMarketTime=... regularMarketPrice=129.6` and `DAILY-CHK ASTS current=125.5 prev=119.7 pct=4.85`, while IBKR showed -3.5%. During extended hours the code took `current = lastClose` (the pre-market candle, correct) but `previous = meta.previousClose` (119.7 = the close from TWO sessions ago). The correct baseline is YESTERDAY's regular close = `meta.regularMarketPrice` (129.6). Real math (125.2-129.6)/129.6 = -3.4% ✓ matches IBKR; app's wrong math (125.5-119.7)/119.7 = +4.85% ✗. Fix in `IvService.fetchYahooPrice` (v8 chart path): introduced `isExtendedHours = lastTs != null && lastTs > regMarketTime && lastClose>0`; when true → `current = lastClose`, `previous = regularPrice ?: previousClose`; else (regular session / closed) → `current = regularPrice ?: lastClose`, `previous = previousClose` (UNCHANGED regular-hours behavior). Updated PREPOST_DEBUG FINAL log to print `isExt`, current, previous, regular, prevClose, lastClose. NOTE this is the v8 single-ticker path only; the v7 batch `fetchYahooPrices` (workers/alerts) was not touched.
S0'2 top-movers show price: top gainers/losers rows previously showed only "TICKER ±x%". Added the current stock price between ticker and percent (e.g. "ASTS $125.20 -3.5%"). Threaded a new `stockPriceByTicker: Map<String,Double>` (uppercase ticker → current price) through `DashboardSummary` (DomainModels.kt), built in `ReportGenerator` from the stock snapshot's `current` field right after `stockMovers`, and rendered in `DashboardScreen` movers rows: ticker (Hebrew RTL → visual right) + LTR `$price` (textSecondary) + LTR signed `%` (bold, pnl color). Sign stays left of the number per CLAUDE.md.

### Group T prime - COMPLETE (commit 752b8cb)
T1 income-journal weekly table column separation: the two amount columns ("נאספו" blue / "מומשו" green) still looked ambiguous (a number appeared to float between them) — root was that the LTR-wrapped number cell used `textAlign.End` (number at the right edge of its slot) while the RTL header used `textAlign.End` (label at the LEFT edge of the same slot), so label and number sat at opposite ends. Rewrote both header and data rows in `PremiumIncomeScreen.kt` WeekCard: each amount lives in its own `Box(Modifier.weight(1.3f), contentAlignment = Alignment.CenterEnd)` with the number LTR-wrapped inside, and a `Box(Modifier.width(1.dp).height(16/14.dp).background(divider))` vertical line sits BETWEEN the נאספו and מומשו cells in both rows. Now header + number align to the same edge within each cell and the divider makes the boundary unambiguous.
T2 "פרמיות שמומשו:" sub-label RTL: the realized sub-detail column used `horizontalAlignment = Alignment.End`, which in a globally-RTL screen = visual LEFT, so the Hebrew label pulled left like English. Changed to `Alignment.Start` (= visual RIGHT in RTL) to match the premium column. Label + lines now start from the right.
T3 top-movers price/percent spacing: gainers had price glued to the % while losers had a gap (both relied on a fragile trailing space inside an LTR span). Made both identical — dropped the trailing space in the price string and added an explicit `Spacer(Modifier.width(6.dp))` between the price block and the percent block in BOTH gainer and loser rows.
T4 losers percent minus sign: the "N יורדות (x%)" line showed no minus. Root: `avgLossPercent` is the average of `profitPercentOnPosition` (which for OPEN positions uses `expectedProfitAtExpiration`) over positions filtered by *unrealized* < 0 — so it can resolve POSITIVE. Forced the sign for the down-positions label: `(-${"%.1f".format(kotlin.math.abs(summary.avgLossPercent))}%)`. (No P&L logic touched — display only.)
T5 session badge to card header: the "פרה"/"אחה״צ" pre/post badge was rendered next to the top-gainers title. Hoisted the nyTime/isPremarket/isAfterHours/sessionBadge computation above the open-positions card title row and rendered the badge there (after "N פוזיציות פתוחות"); removed it from the gainers title.
T6 CC reminder excludes no-options-market tickers: a held stock with no listed options was showing in the CC reminder. No offline options-chain flag exists and ReportGenerator has no IvCacheDao, so used the heuristic — a ticker "has an options market" only if it appears in `allPositions` (any open/closed option trade we've ever made). Added `val tickersWithOptions = allPositions.map { it.ticker.uppercase() }.toSet()` and a `.filter` guard (with a `CC_SKIP` log) at the top of the `ccReminderItems` filter. Also cleaned the header text grammar to "💡 תזכורת Covered Call · N טיקרים זמינים". Accepted tradeoff: a stock you hold with a real options market but never traded options on would also be excluded — acceptable per the user's heuristic.

### Group U prime - COMPLETE (commit 550dbaa)
Follow-ups to T prime, all in `PremiumIncomeScreen.kt` + `DashboardScreen.kt`. No realizedPnL / DB changes.
U1 weekly-table headers RTL: the "נאספו"/"מומשו" header Boxes (and the per-day number Boxes) used `contentAlignment = Alignment.CenterEnd` — in global RTL End = visual LEFT, so the Hebrew headers sat on the left and read "LTR-like". There was no LTR wrapper to remove (single Hebrew words don't reorder). Fix = flipped all four amount Boxes (2 header + 2 data, the `Box(Modifier.weight(1.3f), …)`) from `CenterEnd` → `CenterStart` (Start = visual RIGHT in RTL) via a replace-all. Headers now right-anchored (RTL-natural) AND the numbers move with them so each amount still sits under its header (T1 divider preserved between the two columns).
U2 realized column always LEFT: the day sub-detail rendered each side only `if (showPremiumCol)/(showRealizedCol)`, so when there was no collected premium the realized Column became the only child and shifted to the right. Restructured to TWO always-present fixed-weight slots — `Box(Modifier.weight(1f)){ if (showPremiumCol) Column(Start){…} }` (RIGHT) + `Spacer(8.dp)` + `Box(Modifier.weight(1f)){ if (showRealizedCol) Column(Start){…} }` (LEFT). Each half is reserved regardless of content, so realized stays in the LEFT half and premium in the RIGHT half. (Done via PowerShell line-splice reusing the original inner content verbatim — it contains `\"` in "סה\"כ:" which the Edit tool mangles.)
U3 centered stat cards: "חשיפת דלתא" and "הפסד מקסימלי ברצף" cards had `Column(CenterHorizontally)` but no `fillMaxWidth`, so the wrap-content column hugged the RTL start edge. Added `Modifier.fillMaxWidth()` to both Columns and `Modifier.fillMaxWidth()` + `textAlign = TextAlign.Center` to their label/value/sub Texts (same pattern as the R2 StatMini fix).
U4 losers-summary minus placement: the "N יורדות (-X.X%)" line (T4) was a single Text NOT wrapped in LTR, so in RTL the parens/minus could mis-place. Split into a Hebrew "N יורדות " Text + an LTR-wrapped "(-X.X%)" Text so the minus stays glued left of the digits. (The per-row losers % was already inside an LTR span and rendered "-3.5%" correctly — left as-is.)
U5 gainers-vs-count VERIFIED (no bug): confirmed `topGainers = stockMovers.filter { it.second > 0 }` where `it.second` is the daily *price* change — i.e. tickers that moved UP in price today. "N עולות (X%)" is `profitablePositionCount`/`avgProfitPercent` (open-position P&L) — a DIFFERENT metric. An empty top-gainers list next to a non-zero "N עולות" is correct when nothing moved up in price (e.g. all red pre-market). No behavior change; added a clarifying MOVERS_UI log only.

### Group V prime - COMPLETE (commit bfe3f37)
V1 realized color = pnlColor: in `PremiumIncomeScreen.kt` the day sub-detail "פרמיות שמומשו:" header was hard-coded `PrimaryGreen` even on loss days. Changed to `pnlColor(dayRealized)` (green ≥0, red <0). The realized VALUES (per-position lines, the day "סה\"כ", the weekly "סה\"כ מומשו", the week-total row) ALREADY used `pnlColor` — only the sub-header was wrong. Left the small weekly "מומשו" column-header label green (it's a column label, not a value, per the prompt's "minimal" note).
V2 centered weekly headers: the "נאספו"/"מומשו" header Boxes were `contentAlignment = Alignment.CenterStart` (U1's right-anchor). User wanted them centered over their columns. Changed ONLY the two header Boxes to `Alignment.Center` (matched each by its inner Text so the per-day data Boxes — same `Box(weight 1.3f, …)` string — were NOT touched and keep their CenterStart number alignment).
V3 UnrealizedPnlScreen column overflow (the "P&L לא ממומש" page): P&L numbers ran into the "נוכחי" column. Root cause = the row used `horizontalArrangement = Arrangement.SpaceBetween` WITH weighted children — weights fill the row so SpaceBetween leaves ZERO inter-column gap, and a wide P&L could touch the adjacent column. Fixes (header + data rows kept identical): switched both to `Arrangement.spacedBy(8.dp)`; re-weighted to give P&L room (ticker 1.2→1.0, premium/current 1→0.9, P&L 1→1.3); added `maxLines = 1` to the premium/current/P&L number Texts. Bottom total row regrouped from SpaceBetween to `Arrangement.End` with a Spacer so "סה\"כ: <amount>" sits together on the right (amount near the P&L column). The top summary card was already correct.
V4 PortfolioBreakdownScreen "לא נכלל" (excluded) table cleanup: it used raw mismatched weights (1/0.5/0.7/0.8/1) with no header and a 2-column total that didn't line up with the 5-column rows. Rebuilt the excluded card's Column to mirror the INCLUDED table exactly — a `PlainHeaderCell` header row (טיקר/מניות/מחיר/שווי/מקור), `DataCell(W_*)` data rows using the shared `W_TICKER/W_SHARES/W_PRICE/W_VALUE/W_SOURCE` constants, dividers/spacing, and a total row (`"סה\"כ לא נכלל"` weight W_TICKER + Spacer W_SHARES+W_PRICE + LTR amount weight W_VALUE+W_SOURCE) so the excluded total aligns under the value column. Muted excluded colors (textTertiary/textSecondary) preserved. (PowerShell line-splice — the block contains `\"`.)

### Group W prime - COMPLETE (commit 65252dc)
W1 historical monthly target (THE calc bug): the annual-target monthly breakdown showed past months with the CURRENT/latest target (e.g. Jan = 5088) instead of the target in effect THEN. Two separate code paths both fell back to "latest": `ReportGenerator.getMonthlyReport` did `monthTarget ?: getLatestTarget()`, and `AnnualTargetViewModel.loadYear` (the per-month list — it does NOT use getMonthlyReport, it has its own loop) used a two-pass average-of-explicit-targets fallback. Fix: new read-only DAO query `getEffectiveTargetForMonth(yearMonth)` = `SELECT * FROM monthly_targets WHERE year_month <= :yearMonth ORDER BY year_month DESC LIMIT 1` (the most recent target stored at-or-before that month). Added a repo passthrough. `getMonthlyReport` now uses `monthTarget ?: getEffectiveTargetForMonth(yearMonth)`; `loadYear` replaced its two-pass average with a single pass calling `getEffectiveTargetForMonth(month)` per month. Result: a past month shows the target that existed then (0 if none existed yet), the current month its current target, and future months the latest target. `ANNUAL_TARGET` log added in both paths. NO schema change (read-only query). Left `getDashboardSummary`'s line-170 `targetForMonth ?: getLatestTarget()` alone (current-month dashboard already uses the explicit current target; out of W1 scope).
W2 unrealized P&L total RTL: V3 had used `Arrangement.End` inside an LTR wrapper, so the total read "amount סה\"כ" (number then label). Removed the outer LTR wrapper so the Row inherits global RTL, switched to `Arrangement.SpaceBetween` → Hebrew "סה\"כ:" on the visual RIGHT, the number in its own LTR span on the LEFT, with `maxLines = 1, softWrap = false` (no ellipsis/truncation; large numbers render fully). `UnrealizedPnlScreen.kt`. (PowerShell splice — block has `\"`.)
W3 "אסטרטגיה" → "אסט'": abbreviated the strategy column header on the unrealized P&L page so it fits.
W4 center journal day numbers: V2 centered the weekly headers but the per-day number Boxes were still `CenterStart`. Flipped the two data Boxes (`Box(weight 1.3f, …)`) to `Alignment.Center` (replace-all; the header Boxes were already Center) so each נאספו/מומשו number sits centered under its centered header. Vertical divider between columns kept.
W5 annual page grammar: `ProjectionRow` label "עברו מהשנה:" → "מתחילת השנה:" (pairs naturally with the "X ימים (Y%)" value).

### Group X prime - COMPLETE (commit 635543a)
Follow-ups to W1's effective-target work + annual-page polish. NO realizedPnL / DB schema changes (new DAO queries are read-only).
X1 historical targets for Jan/Feb/Mar 2026 are MISSING from the DB (ANNUAL_TARGET log: effective=null for 2026-01/02/03; first stored = 2026-04). The user set them but they were lost (overwritten pre-J', or never persisted per-month). Two parts:
  - X1a fallback: a month that predates ANY stored target now falls back to the EARLIEST stored target (not 0). New read-only DAO `getEarliestTarget()` (`ORDER BY year_month ASC LIMIT 1`) + repo passthrough; applied in BOTH `getMonthlyReport` (`monthTarget ?: getEffectiveTargetForMonth ?: getEarliestTarget`) and `AnnualTargetViewModel.loadYear`. `ANNUAL_TARGET` log now prints `viaEarliestFallback`. So Jan/Feb/Mar show the earliest target (e.g. 5116) until real values are entered.
  - X1b edit a PAST month's target: Settings target editor was locked to `YearMonth.now()`. Added `SettingsViewModel.setEditingMonth(month)` (updates `currentMonth` — already the field that `save()`/`autoSave()` write to — and loads that month's stored target, or CLEARS the target fields if none, with NO fallback-to-latest and NO auto-saved default). UI: replaced the static "יעד חודשי - <month>" header with a month selector (prev/next chevrons + LTR "MM/YYYY" + "עורך יעד לחודש:" sublabel). So the user can switch to January, type 2000, save; February 2500; etc.
X2 "מתחילת השנה" word order: value rendered "ימים (40.5%) 148" because its first STRONG char (Hebrew "ימים") made the span resolve RTL even inside the ProjectionRow's LTR wrapper, flinging the leading number to the end. Prefixed the value string with `‎` (LRM) so the LTR number leads → "148 ימים (40.5%)". (Wrote the literal `‎` escape via PowerShell since the Edit/JSON layer turns `‎` into the actual char.)
X3 "פער צפוי" double-plus: code did `(if (yearGap>=0) "+" else "") + formatCurrency(yearGap)` but `formatCurrency` ALREADY signs (+/-), giving "++$X". Removed the manual prefix → single sign.
X4 monthly breakdown "realized / target": the realized Text and the "/ target" Text were two separate LTR-wrapped Texts inside an RTL Row, so they laid right-to-left (slash/target flipped left of realized). Wrapped the WHOLE group in one `CompositionLocalProvider(Ltr) { Row { realized; "/"; target } }` so it reads "$realized / $target" left-to-right.
ALSO added a cross-app rule to `shared/conventions.md` ("Number stays adjacent to its Hebrew noun") capturing the X2 LRM/single-LTR-Row lesson.

### Group Y prime - COMPLETE (commit 2a49f28)
Y1 annual "days elapsed" grammar (the persistent one): the value STILL rendered "ימים (40.5%) 148" even after X2's LRM prefix + the ProjectionRow's LTR wrapper — a concatenated `"$days ימים ($pct%)"` string bidi-reorders because the Hebrew word sits BETWEEN numbers, and a single LRM at the start isn't enough. Real fix: stop concatenating — replaced that one `ProjectionRow(...)` in `AnnualTargetScreen.kt` with an inline `Row { Hebrew label (weight 1f); CompositionLocalProvider(Ltr){ Row { Text(days); Spacer; Text("ימים"); Spacer; Text("($pct%)") } } }`. Each piece is its own Text in a forced-LTR Row, so the visual order is guaranteed: `148  ימים  (40.5%)`. Kept the W5 label "מתחילת השנה:" (did not revert to "עברו מהשנה:"). The other ProjectionRows use pure `formatCurrency` (LTR-clean) so they were fine; line ~254's "💡 כדי להגיע ליעד: $X נוספים בחודש" hint (number mid-sentence, Hebrew both sides) was left as-is — not flagged, and the separate-pieces pattern doesn't fit a full sentence. Reinforces the conventions.md rule added in X'.
Y2 target editor: explicit save button instead of per-keystroke auto-save. The Settings target fields were in the autoSave `LaunchedEffect` key list, so every keystroke debounce-saved — annoying, and risky with the X1b past-month editor (a freshly-switched blank month could auto-write a default 2000). Changes in `SettingsScreen.kt`: (1) removed the 8 target/portfolio keys (portfolioValue, capitalAllocated, targetAmount, targetPercent, livingTarget, livingCurrency, growthTarget, growthCurrency) from the autoSave `LaunchedEffect` — it now keys only on rates + API keys + availableFunds; (2) stripped the `saveMonthlyTarget` write (and the target readback log) OUT of `autoSave()` so it never persists the target; (3) `save()` (the button path, writes to `currentMonth` = the X1b edited month) now also sets `dataMessage = "היעד נשמר"` for a Toast; (4) added a `Button("שמור יעד")` at the end of the target editor section. Net: typing/switching months no longer saves; target persists ONLY on the button. Non-target settings (rates, API keys, funds) still auto-save exactly as before. This removes the X1b auto-default-write caveat.

### Group Z prime - COMPLETE (commit cecb3e2)
Z1 annual days grammar (third attempt — finally deterministic): the Y1 separate-Texts forced-LTR Row STILL rendered "(40.5%) ימים 148" reversed on device. Replaced it with a single `ProjectionRow` whose value string is wrapped in a Unicode LEFT-TO-RIGHT ISOLATE — `⁦` (LRI) … `⁩` (PDI): `ProjectionRow("עברו מהשנה:", "⁦$daysPassed ימים ($daysPct%)⁩")`. The isolate forces LTR at the character level regardless of the surrounding RTL screen, so the visual order is locked to "148 ימים (40.5%)". Reverted the label to "עברו מהשנה:" per the user's explicit Z1 instruction (was W5's "מתחילת השנה:"). NOTE: wrote the literal `⁦`/`⁩` escapes via PowerShell — the Edit/JSON layer renders typed `⁦` as the actual invisible isolate char; used char-code construction (`[char]0x5C + 'u2066'`) to get visible ASCII escapes (same lesson as X2's LRM).
Z2 DIAGNOSTIC ONLY (no logic change): after the user entered historical Jan/Feb/Mar targets, FUTURE months show 2000 (the earliest) instead of the latest. Suspect the stored `year_month` format breaks string ordering — e.g. `getLatestTarget()` does `ORDER BY year_month DESC LIMIT 1`, and if months are stored as "2026-1".."2026-9","2026-04" inconsistently (no zero-pad), lexicographic ordering returns the wrong "latest"/"earliest". Added (to `AnnualTargetViewModel.loadYear`, before the month loop) an `ANNUAL_TARGET` "DIAG" log dumping `latest`/`earliest` (yearMonth:amount) and ALL stored targets' exact `year_month` strings; enhanced the per-month log to include `effective?.yearMonth`. New read-only repo passthrough `getAllTargetsOnce()` → `monthlyTargetDao.getAllOnce()`. **NO target-selection logic changed — waiting on the device DIAG log to confirm the year_month format before fixing.** `YearMonth.toString()` produces zero-padded "2026-01", so if stored values are NOT zero-padded the mismatch is the likely culprit.
Z3 removed ALL auto-save in Settings; per-section "שמור" buttons. Deleted the debounced multi-key autoSave `LaunchedEffect` (the `autoSave()` VM fun is left in place but is now dead/uncalled). Added VM `saveRates()` (USD→ILS / USD→THB exchange rates) and `savePortfolio()` (available funds to prefs + portfolioValue onto the month's target, only if a target already exists — no spurious default target). Added "שמור פורטפוליו" button (→ savePortfolio) under the portfolio section and "שמור שערים" button (→ saveRates) under the rates section; target keeps "שמור יעד" (Y2 → save()), AI keeps "שמור הגדרות AI" (saveAiSettings), IV keeps "שמור הגדרות IV" (saveIvSettings). Each shows a Toast via `dataMessage`. Loading on entry still populates fields. NOTE left as-is (flagged, not changed): the ₪/฿ target inputs (`updateTargetAmountIls`/`Thb`) still persist to prefs on keystroke inside their updaters — a separate instant-save path, not the autoSave LaunchedEffect.

### Group AA prime - COMPLETE (commit 4eb0f9f)
AA1 days grammar — REAL LRI/PDI chars (fourth attempt, definitive): Z1's value was `"⁦$days ימים ($pct%)⁩"`. Byte-check showed a SINGLE backslash `⁦` (which Kotlin SHOULD compile to U+2066), yet it still rendered reversed on device. Stopped relying on escape processing entirely — now builds the actual control chars from code points: `val lri = String(Character.toChars(0x2066))` / `val pdi = String(Character.toChars(0x2069))`, then `ProjectionRow("עברו מהשנה:", "$lri$days ימים ($pct%)$pdi")`. This puts the genuine LEFT-TO-RIGHT ISOLATE / POP DIRECTIONAL ISOLATE chars in the runtime string, immune to any `\u`/escape mangling. **Lesson: for bidi control chars in a string, use `String(Character.toChars(0xNNNN))`, never `\uNNNN` literals — the edit/build pipeline can leave them as literal text.**
AA2 future-month target uses the CURRENT month (root cause found via Z2's DIAG log): stored targets were `2026-01:2000, 02:2500, 03:3000, 04:5116, 05:5061, 06:2000` — an accidental future-dated `2026-06:2000` record. For a future month, `getEffectiveTargetForMonth(month)` matched `year_month <= month` and picked up the 2026-06 record, so Jul–Dec showed 2000. (So the bug was NOT the year_month string-format hypothesis from Z2 — it was a real future-dated row.) Fix in `loadYear`: `val nowMonth = YearMonth.now(); val lookupMonth = if (month.isAfter(nowMonth)) nowMonth else month;` then look up `getEffectiveTargetForMonth(lookupMonth)`. Past month → its then-current target; current → current; future → the current month's effective target (5061), NOT the future-dated 2026-06 row. The accidental 2026-06 record is now harmless to the annual projection (it only shows if June itself is viewed). Z2's diagnostic logs left in place.
`getLatestTarget()` call-site review (per AA2 instruction — NOT changed, flagged for review): `FlexSyncWorker.kt:693` (carry-forward of prior breakdown on new-month sync — a future-dated record could carry the wrong breakdown forward, low risk), `ReportGenerator.kt:170` (dashboard current-month: `getTargetForMonth(now) ?: getLatestTarget()` — current month has its own record so the fallback rarely fires; if it did with a future-dated row it could mislead, but currently fine), `SettingsScreen.kt:336` (settings load fallback), `AddPositionViewModel.kt:150/337/868` (add-position default target). None changed; none currently broken by the 2026-06 row, but `getLatestTarget()` is inherently future-dated-record-vulnerable if more such rows appear — worth a future hardening pass.

### Group AB prime - COMPLETE (commit 54c1730)
AB1 days grammar — forced-LTR Row of separate single-token Texts (the LAYOUT solution; FIFTH attempt, hopefully final): every BIDI/isolate approach failed on device — X2 LRM, Z1 `⁦` escape, AA1 `String(Character.toChars(0x2066))` real LRI chars — all reordered (AA1 showed "148 (40.5%) ימים"). Compose `Text` does NOT honor U+2066 isolates for ordering. Switched to a pure LAYOUT solution: in `AnnualTargetScreen.kt`, the value is an inner `Row` wrapped in `CompositionLocalProvider(LocalLayoutDirection provides LayoutDirection.Ltr)` containing THREE separate Texts — `Text("$daysPassed")`, `Text("ימים")`, `Text("($daysPct%)")` — each a single token (number alone / Hebrew word alone / parenthesized percent alone) with `Spacer(4.dp)` between. Row child placement honors `LocalLayoutDirection`, so children arrange left→right deterministically (148, ימים, (40.5%)); no in-Text bidi mixing can reorder them. **LESSON: for a Hebrew-word-between-numbers value, NEVER rely on bidi controls — use a forced-LTR Row of single-token Texts.** (NOTE: Y1 also used a forced-LTR Row but reportedly still reversed — this AB1 version is the explicit single-token-per-Text form the user re-prescribed; if it STILL reverses it's a deeper Compose/device issue needing a screenshot.)
AB2 social toggle bare-ticker match: `DashboardScreen.kt` "רק טיקרים בתיק" filter only matched `[@$]TICKER`, so a post mentioning a portfolio ticker as a BARE word (e.g. "BBAI" with no $/@) was filtered OUT. Rewrote the `portfolioFiltered` predicate: for each `relevantTickers` ticker, match if the uppercased post `contains("$"+ticker)` OR `contains("@"+ticker)` OR (ticker.length >= 3) a word-boundary regex `\b<ticker>\b`. The length>=3 gate keeps bare matching off 1-2 char tickers (avoids English-word false positives like "ON"/"IT"); short tickers still require $/@.
AB3 annual growth bar = 0 — DIAGNOSTIC ONLY (stopped at diagnosis per instruction): the growth ("גדילה") bar lives in `SegmentedTargetBar` (DashboardScreen ~2464), shown on the dashboard monthly card (~1789) and annual block (~2001), both guarded by `if (monthlyLivingTarget > 0 && monthlyGrowthTarget > 0)`. Root mechanism IS visible in code: `growthFilled = (realized − livingTargetUsd).coerceAtLeast(0)` converted to growth currency — i.e. growth fills ONLY with realized BEYOND the living target (living-first design from E2/H'/I'). So if realized hasn't passed the living target, the growth segment is correctly 0%. Added an `ANNUAL_TARGET` "growth bar: realized=… livingTarget=… livingFilled=… growthTarget=… growthFilled=… growthSpillUsd=…" log inside SegmentedTargetBar. NO logic change. NEXT SESSION: read the device log — if `growthTarget>0` but `growthFilled=0` because realized < living, it's working as designed (decide whether the user wants proportional fill instead of living-first); if `growthTarget=0` the bar shouldn't render at all (data/breakdown not set). `monthlyGrowthTarget` source = current-month effective target.growthTarget (`ReportGenerator:821`).

### Group AC prime - COMPLETE (commit ba13041)
AC1 days grammar — RTL order (the direction was BACKWARDS the whole time): the desired Hebrew reading is RIGHT→LEFT — 148 on the RIGHT, ימים middle, (40.5%) on the LEFT. Every prior attempt (X2 LRM, Z1/AA1 isolates, AB1 forced-LTR Row) pushed 148 to the LEFT (English order), which is what looked "reversed". The real fix: render the value group in the screen's NATURAL RTL (do NOT force LTR on it). In `AnnualTargetScreen.kt` the inner value `Row` now inherits global RTL, so its children place right→left: `Text("$daysPassed")` rightmost, `Text("ימים")` middle, percent leftmost. Only the percent `Text("($daysPct%)")` is wrapped in `CompositionLocalProvider(LocalLayoutDirection provides LayoutDirection.Ltr)` so its parentheses render literally "(40.5%)" not ")40.5%(" — the LTR wrap affects only that Text's internal glyphs, NOT its position (still the 3rd/leftmost RTL child). **LESSON (the actual one): a Hebrew label+number reads RIGHT-to-LEFT — the number belongs on the RIGHT. Use natural RTL layout for the value group; wrap ONLY a parenthesized/sign-bearing sub-token in LTR for literal glyphs. Forcing LTR on the whole group is the bug, not the fix.**
AC2 social filter includes stock holdings: a $BBAI post was hidden under "רק טיקרים בתיק" because `relevantTickers` was built only from `openPositions` (option positions) + watchlist — BBAI is a pure STOCK holding with no open option, so it wasn't in the set. Added `socialHoldingTickers = summary.stockPriceByTicker.keys.map { uppercase }` to `relevantTickers` (now openPositions ∪ watchlist ∪ stock-holdings). With AB2's bare-ticker matching already in place, the $BBAI post now matches and shows. Added a `SOCIAL_FILTER` log printing relevantTickers + the holdings subset.

### Group AD prime - COMPLETE (commit f8c9770)
AD1 annual living/growth bars = SUM of per-month fills (the real AB3 fix): the dashboard ANNUAL `SegmentedTargetBar` (DashboardScreen ~2014) was called with `realized=annualRealized, livingTarget=monthlyLiving*12, growthTarget=monthlyGrowth*12`, and SegmentedTargetBar does living-FIRST — growth fills only AFTER the full 12× living is met. So YTD realized hadn't beaten 12 months of living → annual growth = 0 (this was AB3's "growth bar = 0", working-as-designed but unintuitive). Fix: in `ReportGenerator.getDashboardSummary` (after `annualRealized`), loop the 12 months of the current year and accumulate per-month fills: `monthRealizedUsd` (this-year closed positions partitioned by close/open month, same source as annualRealized) vs that month's effective target (`getEffectiveTargetForMonth(min(month, now)) ?: getEarliestTarget()`, AA2 cap applied), converting living/growth targets to USD via the now-public `MonthlyTargetCalculator.toUsd`. Per month: `livingFill = min(realized, livingUsd)`, `growthFill = (realized − livingUsd).coerceIn(0, growthUsd)`. Summed into 4 new `DashboardSummary` fields (`annualLiving/GrowthFilledUsd`, `annualLiving/GrowthTargetUsd`, all USD). New `SegmentedTargetBarSummed` composable (DashboardScreen) renders those pre-summed values directly (NO living-first recompute), converting USD → the configured display currency, reusing the existing `OverlayProgressBar`. Now each month's growth spill-over counts toward the annual growth bar — consistent with the monthly bar. `ANNUAL_TARGET "annual fills:"` log added. (The monthly bar at ~1801 is unchanged — it was already correct per single month.)
AD2 social toggle rename: "רק טיקרים בתיק" → "הטיקרים שלי" (it now spans portfolio + watchlist + stock holdings after AC2, so "my tickers" is more accurate than "only portfolio tickers").

### Group AE prime - COMPLETE (commit 9e1b2d2)
All in `DashboardScreen.kt`. No realizedPnL / DB changes.
AE1 (NEW28) compact monthly target card: removed the bottom "סה\"כ … נותר: $X / ✓ הושג היעד" total Row that rendered under the SegmentedTargetBar in the segmented (living>0 && growth>0) branch — it added height. Instead the PercentPill Row above the bar now shows "✓ הושג היעד" (PrimaryGreen, bold) to the LEFT of the pill when `monthlyPctClamped >= 100.0`. When not met, just the pill (no "נותר" row). NOTE the ELSE branch (single fallback bar, used only when no living/growth breakdown is configured — NOT this user's case) was left as-is: its "$realized / $target · נותר / ✓ הושג" text is an OVERLAY centered INSIDE the bar (adds no height, already compact), so there was no separate row to remove. Minor: when met in that fallback branch the pill says "✓ הושג היעד" and the in-bar overlay also says "✓ הושג $X / $Y" — acceptable in the rarely-shown fallback; flagged, not changed.
AE2 (NEW30) top-losers column RTL (mirror gainers): the losers `Column` used `horizontalAlignment = Alignment.End` (= visual LEFT in RTL) and its title Row used `horizontalArrangement = Arrangement.End` with [chevron][title] order, so the whole column hugged the left and read LTR-ish. Rewrote the losers Column to MIRROR the gainers column exactly: default Start alignment (= visual RIGHT in RTL), title Row `[Text(title)][optional chevron]` (no End arrangement), and each per-row `[ticker][price(LTR)][pct(LTR)][IV]` (was `[IV][ticker][price][pct]`). Only LossRed vs PrimaryGreen differs now. Top-losers reads right-hugging like top-gainers.

### Group AF prime - COMPLETE (commit 3b0aea4)
AF1 annual growth bar = sum of RAW monthly spills (fixes an AD1 bug): AD1 capped each month's growth fill at that MONTH's growth target (`coerceIn(0.0, mGrowthUsd)`), so each month maxed at ~$2000 → device showed $4000 (2×cap) instead of the correct ~$5953. In `ReportGenerator.getDashboardSummary` per-month loop: living is still capped per month (`min(realized, living)` — living can't exceed its monthly target, the rest spills), but growth now sums the FULL spill `(monthRealizedUsd − mLivingUsd).coerceAtLeast(0.0)` with NO upper cap per month; after the loop the summed living/growth are each `coerceAtMost` their ANNUAL target for the bar ratio. So a big month's spill carries into the annual growth bar. (User hand-verified: per-month realized USD→THB − 100k THB living → back to USD = that month's growth.) Log updated to "annual fills (uncapped-per-month)".
AF2 (NEW29) % on the income-journal month bars: each "רווח לפי חודש" bar now shows that month's realized as % of that month's monthly target, centered on the bar (like the dashboard PercentPill). Added `OverallReport.profitPercentByMonth: Map<YearMonth, Double>`, built in `getOverallReport` (suspend, has repository) by looping `profitByMonth` and, per month, `percent = profit / effectiveTarget.targetAmount * 100` where the effective target = `getEffectiveTargetForMonth(min(month, now)) ?: getEarliestTarget()` (AA2 future-cap). Rendered in `PremiumIncomeScreen` as an LTR-wrapped 9.sp bold Text centered in the bar's track Box; the R6 `$`-amount column to the right is unchanged. (Used `targetAmount` per the prompt; months with targetAmount=0 show 0%.)

### Group AG prime - COMPLETE (commit d3ddc3b)
AG1 annual living/growth bars count breakdown months ONLY (fixes AF1's inflation): the device log showed `livingFilled=6121/27548 growthFilled=16432/18000` — living right (2 breakdown months ≈ $3061 each) but growth way over (should be ~$5953). Root: Jan/Feb/Mar were entered via the X1b editor as a single COMBINED target (targetAmount set, livingTarget=0/growthTarget=0). With no living target, each of those months' growth spill = `realized − 0` = its full realized → it all piled into the annual growth bar. Fix in `ReportGenerator.getDashboardSummary` per-month loop: guard `if (mLivingUsd > 0.0 && mGrowthUsd > 0.0)` — only months with a REAL living/growth breakdown contribute to BOTH the filled sums AND the target sums; others log "SKIPPED". So only Apr/May (the breakdown months, + future months carrying the current breakdown with realized=0) feed the bars. Annual caps after the loop unchanged. Log now "annual fills (breakdown-only)".
AG2 (NEW31) total progress bar replaces the percent pill: in the monthly target card, swapped the `PercentPill` (the "104%" chip with a background) for a real TOTAL progress bar — a `Box` track (surfaceVariant, 18.dp, rounded) with an inner fill `Box(fillMaxWidth(pctFraction.coerceAtLeast(0.02f)))` in `AccentBlue` that grows with `monthlyPctClamped` (clamped 0..100% for the fill), the "%" centered on it, and to its RIGHT either "נשאר $X" (textSecondary) or "✓ הושג היעד" (PrimaryGreen) when met. Explicitly LEFT the מחיה/גדילה `SegmentedTargetBar` (and its colors) untouched below — the card now shows three bars: total / מחיה / גדילה. (`PercentPill` import/usage removed from this spot; the component still exists for other screens.)

### Group AH prime - COMPLETE (commit dbdb9bf)
Refined the AG2 total progress bar in the monthly target card (`DashboardScreen.kt`). Two tweaks, no logic/data change:
1. Bar FILL color now uses the SAME tiered colors the old `PercentPill` used (copied from `ui/components/PercentPill.kt`): `pillFraction >= 1.0 → Color(0xFF2E7D32)` dark green, `>= 0.75 → 0xFF689F38` olive, `>= 0.50 → 0xFFF9A825` amber, `>= 0.25 → 0xFFEF6C00` orange, else `0xFFC62828` red. Replaces the flat AccentBlue from AG2. Added `import androidx.compose.ui.graphics.Color` (file previously used FQN).
2. Status label ("נשאר $X" / "✓ הושג היעד") moved to the RIGHT of the bar — in the globally-RTL Row the first child is rightmost, so the label is now the FIRST child and the bar (weight 1f) fills the space to its left. The track `Box` is wrapped in `CompositionLocalProvider(LocalLayoutDirection provides LayoutDirection.Ltr)` so the fill `Box(fillMaxWidth(pctFraction))` grows from the LEFT edge toward the label (without the wrap, the RTL track would anchor the fill to the right). `Modifier.weight(1f)` resolves fine inside that provider via the lexical RowScope (same mechanism as `align` inside a Box). The % text stays white-centered on the bar.
NOTE: the מחיה/גדילה `SegmentedTargetBar` and its green/blue colors are explicitly untouched — only the TOTAL bar changed. The `PercentPill` component still exists for other screens; it's just no longer used on this card.

### Group AI prime - COMPLETE (commit 0755dd3) — NEW27 per-ticker volatility alert threshold
Replaced the flat 5% abnormal-move alert gate with a per-ticker threshold = `2 × (ticker's avg daily move %)`, clamped `[3%, 25%]`; falls back to flat 5% until the vol cache is populated. So a 3x-leveraged ETF (avg ~5.5%) needs ~11% to alert while a calm ticker (avg ~2%) alerts at 4% — no more daily false alarms on volatile names. Three pieces, NO DB schema change (cached in the existing snapshot JSON):
1. **`IvService.fetchAvgDailyMovePct(ticker)`** (new suspend fun, mirrors `fetchYahooPrice`'s HTTP setup — HttpURLConnection, UA Mozilla/5.0, 10s timeouts, Yahoo v8 chart `interval=1d&range=1mo`). Computes mean absolute day-over-day % change over ~1mo of daily closes; needs ≥5 closes else null. Logs `VOL_THRESH "<t> avgDailyMove=X% over N days"`.
2. **`DashboardViewModel.updateVolatilityCache(snapshot, tickers)`** (new private suspend helper). For each ticker, skips if `volComputedDate == today`, else fetches and writes `avgDailyMovePct` + `volComputedDate` into the per-ticker snapshot obj. Returns true if anything changed. Wired into all 4 refresh paths (init, refreshOnResume, pullToRefresh, triggerPriceRefresh): each `if (updated)` → `val volChanged = updateVolatilityCache(...); if (updated || volChanged)`. Fetches at most ONCE per ticker per day.
3. **`ReportGenerator.getDashboardSummary` alert loop**: gate now reads `obj.optDouble("avgDailyMovePct")`, computes `threshold = (2.0 * avgDailyMove).coerceIn(3.0, 25.0)` (else 5.0), logs `VOL_THRESH`, and `if (abs(changePct) < threshold) return@forEach`. The CC/CSP suggestion when-block (still hardcoded ±5.0 branches) is UNCHANGED per instruction; only the note is enriched with `avgSuffix = " (רגיל ~X%)"` appended (`note = note + avgSuffix`). Stale "threshold=5%" header log updated to "per-ticker(2×avgMove, clamp[3,25]%)".
Awaiting device verify: VOL_THRESH logs showing per-ticker thresholds + alert notes carrying the "(רגיל ~X%)" suffix.

### Group AJ prime - COMPLETE (commit 4e221ea) — NEW1 feed shows position-edit field changes
The activity-feed entry for an edited position used to show a static `"Strike ${strikePrice} • ${numContracts} חוזים"` regardless of what was edited. Now it shows WHAT changed as "old ← new" per changed field. In `AddPositionViewModel.kt` (the `editingPositionId != null` branch), right after `repository.updatePosition(finalMerged)` and BEFORE the `if (finalMerged.status != PositionStatus.DRAFT)` feed block (which shadows the outer `existing` with the activity-event row):
1. Capture `val oldPos = existing` (the pre-edit position; nullable).
2. Build `val changes = mutableListOf<String>()`; if `oldPos != null`, compare strike / numContracts / premiumPerContract / expirationDate / impliedVolatility, adding `"label: old ← new"` only for fields that changed. Doubles use a local `fun dNeq(a,b) = abs((a?:0.0)-(b?:0.0)) > 0.0001` tolerance; contracts use `!=` (Int); expiration uses `!=` (LocalDate); IV nulls render as "—". `"%.2f"` for strike/premium, `"%.1f"` for IV.
3. Description = `if (changes.isNotEmpty()) changes.joinToString(" · ") else "Strike … • N חוזים"` (static fallback kept). Added `ACTIVITY_FEED "POSITION_EDITED <ticker> changes=[…]"` log.
DIRECTION NOTE: written as "old ← new" in source so in the RTL Hebrew feed it reads right→left as old(right)→new(left). If device shows it backwards, flip the arrow to "old → new". Rest of the feed block (title, insert/update of the event row, DRAFT guard) untouched. NO DB schema change.
Awaiting device verify: edit a position's strike/contracts/premium → feed description reads e.g. "Strike: 2.50 ← 3.00 · חוזים: 1 ← 2"; no-change edit keeps the static text; confirm arrow reads from→to in the RTL feed (flip if needed).

### Group AK prime - COMPLETE (commit 43561c9) — CC reminder hyphen + annual card percent-on-bar
Two UI tweaks in `DashboardScreen.kt`, both v2 (prior attempt failed on Edit-tool/encoding + lost agent-memory path; redone fresh with Read+Edit).
- **AK1:** CC reminder header separator changed from middle-dot `·` to plain hyphen `" - "`. Line ~1148: `"💡 תזכורת Covered Call - ${summary.ccReminderItems.size} טיקרים זמינים"`.
- **AK2:** Annual progress card (around `summary.annualProgressPercent`, ~1988) now mirrors the monthly bar style. (1) DELETED the `PercentPill` Row (`Arrangement.End` + `com.dima.optionstracker.ui.components.PercentPill(pct/100.0)`) and its trailing `Spacer(4.dp)`. (2) REPLACED the `progressColor` when-block (old 44BB44/AACC00/FF8800/FF4444) with monthly tier colors: ≥100 → 2E7D32, ≥75 → 689F38, ≥50 → F9A825, ≥25 → EF6C00, else C62828. (3) RE-ADDED a centered LTR percent overlay (`"%.0f".format(pct)%`, labelSmall bold white, `Modifier.align(Alignment.Center)`) inside the 22.dp bar track Box, replacing the old "Percent overlay removed (FIX B2)" comment. The `SegmentedTargetBarSummed` living/growth block below was untouched. Build passed (`:app:compileDebugKotlin`, only pre-existing deprecation warnings).
Awaiting device verify: CC reminder header shows " - " not "·"; annual card has no pill, bar shows centered % in white with tiered color matching its fill level.

### Group AL prime - COMPLETE (commit 973134c) — CC reminder header bidi-scramble fix
Device test of AK1 revealed the single header Text rendered SCRAMBLED: "תזכורת 3 - Covered Call טיקרים זמינים" instead of "תזכורת Covered Call - 4 טיקרים זמינים". Root cause: one `Text` mixing Hebrew + English ("Covered Call") + a digit triggers bidi reordering of the runs. Fix (same approach as AC1 days-grammar): in `DashboardScreen.kt` (~line 1148, inside the Notifications-Icon Row) the single Text was replaced by a **natural-RTL Row** (`Row(verticalAlignment = Alignment.CenterVertically)` — NOT forced LTR) of separate single-token `Text` pieces with 4.dp Spacers: `"💡 תזכורת"` → `"Covered Call"` (LTR-wrapped) → `"-"` → `"${...size}"` (LTR-wrapped) → `"טיקרים זמינים"`. Children place right→left in declaration order, so reading R→L gives "💡 תזכורת Covered Call - 4 טיקרים זמינים". Only the English + number pieces are `CompositionLocalProvider(LocalLayoutDirection provides LayoutDirection.Ltr)`-wrapped so they stay literal; LTR-wrapping does NOT move their position in the RTL row. Surrounding Row (Notifications Icon + Spacer) and the rest of the card untouched. Build passed (`:app:compileDebugKotlin`, only pre-existing deprecation warnings). NO DB/realizedPnL changes.
Awaiting device verify: header reads "💡 תזכורת Covered Call - 4 טיקרים זמינים" in correct order (תזכורת rightmost, then Covered Call, then "- 4", טיקרים זמינים leftmost); no scrambling.

### Group AM prime - COMPLETE (commit 038e43c) — status inside monthly bar + journal LTR content + day dividers + watchlist change%/vol-flag
Four UI fixes from device test of 973134c. NO realizedPnL / DB schema changes.
- **AM1** `DashboardScreen.kt` (~line 1817): the monthly total target bar previously had the "נשאר $X"/"✓ הושג היעד" status as a SEPARATE label to the RIGHT of a weighted bar (made the bar look incomplete even at 108%). Replaced the whole Row with a SINGLE full-width `Box` (`fillMaxWidth().height(20.dp)`, clipped 10.dp, surfaceVariant bg) wrapped in LTR holding three layers: (1) left-grown fill `fillMaxWidth(pctFraction.coerceAtLeast(0.02f))` with the existing tiered `barColor`; (2) centered white % Text; (3) status `Box` aligned `CenterEnd` padding end 8.dp — `totalRemaining>0` → "נשאר $X" Row, else "✓ הושג היעד" — all white, inside the bar. At pct≥100 pctFraction=1.0 → bar fills to the END with "✓ הושג היעד" inside. SegmentedTargetBar (מחיה/גדילה) below untouched.
- **AM2** `PremiumIncomeScreen.kt` (~line 666/696): in both the "פרמיות שנאספו"/"פרמיות שמומשו" day sub-columns, the Hebrew HEADER Text got `Modifier.fillMaxWidth()` + `textAlign = TextAlign.Right` (hugs right), and each per-position English content line (`"${ticker} $strat ... ${formatCurrency()}"`, already LTR-wrapped) got `Modifier.fillMaxWidth()` + `textAlign = TextAlign.Left` (hugs left). `סה"כ:` total sub-rows left as-is. TextAlign already imported.
- **AM3** `PremiumIncomeScreen.kt`: added a full-width `HorizontalDivider(fillMaxWidth().padding(vertical=4.dp), color=divider, thickness=1.dp)` at the END of the `week.days.forEach { day -> ... }` body (after the two-column Row block, before the forEach close) so days within a week are visually separated. Trailing divider after last day accepted.
- **AM4** `WatchlistScreen.kt`: replaced `currentPrices: Map<String,Double>` with `priceInfo: Map<String,WatchPriceInfo>` where `data class WatchPriceInfo(current, changePct, avgDailyMove, abnormal)`. LaunchedEffect now keeps current+previous from `IvService.fetchYahooPrices`, computes `pct=(cur-prev)/prev*100`, fetches `IvService.fetchAvgDailyMovePct(t)`, threshold = `(2.0*avg).coerceIn(3.0,25.0)` else 5.0, abnormal = `abs(pct)>=threshold`; WATCHLIST_VOL log per ticker. `WatchlistCard` param `currentPrice` → `info: WatchPriceInfo?`; next to the price it shows the change % in an LTR span (sign left of number, `pnlColor(pct)`, bold when abnormal) plus " ⚠" (LossRed) when abnormal. pnlColor+LossRed already imported via `theme.*`.
Build passed (`:app:compileDebugKotlin`, only pre-existing deprecation warnings).
Awaiting device verify: (AM1) monthly bar fills fully at ≥100% with "✓ הושג היעד" inside on the right, partial fill + "נשאר $X" inside below 100%, % centered; (AM2) journal Hebrew headers on right, English ticker lines on left; (AM3) long line between days in a week; (AM4) each watchlist row shows day change % (sign left, e.g. "+2.3%"/"-4.1%"), abnormal move (≥2× 21-day avg) bold + ⚠.

### Group AN prime - COMPLETE (commit 3df0a5d) — annual living bar counts non-breakdown months as all-living (option B)
Device test of 038e43c: the annual LIVING bar only counted Apr/May (~200k THB) because the AG1 logic in `ReportGenerator.kt` annual per-month loop SKIPPED any month whose `eff` had no living/growth breakdown (livingTarget=0 OR growthTarget=0). Jan/Feb/Mar were entered as single combined targets ($2000/$2500/$3000, no breakdown) so they never fed the annual living bar. Decision = OPTION B: a month with NO breakdown but WITH a combined `targetAmount>0` is treated as ALL LIVING (growth=0 that month).
- **File/loop:** `ReportGenerator.kt` ~line 446, the `else` branch of `if (mLivingUsd > 0.0 && mGrowthUsd > 0.0)` in `for (m in 1..12)`.
- **Change:** else branch now computes `combinedUsd = if (eff != null && eff.targetAmount > 0.0) MonthlyTargetCalculator.toUsd(eff.targetAmount, eff.currency.name, ilsRateForTarget, thbRateForTarget) else 0.0`. If `combinedUsd>0` → `annualLivingFilledUsd += min(monthRealizedUsd, combinedUsd).coerceAtLeast(0)`, `annualLivingTargetUsd += combinedUsd`, growth untouched; log `ANNUAL_TARGET "month=$monthYM LIVING-ONLY(combined) ..."`. Else log "SKIPPED (no target at all)".
- **Currency field chosen:** `MonthlyTargetEntity` has a combined-target currency field `currency: Currency` (enum `USD/ILS/THB` in PositionEntity.kt:209) — used `eff.currency.name` (matches `toUsd`'s String "USD"/"ILS"/"THB"). NOT hardcoded "USD": the entity has a real currency field, so use it; historical USD months (currency=USD) return the amount unchanged = same as passing "USD", and future ILS/THB combined targets convert correctly. (`targetAmount` comment says "Target in USD" but `eff.currency.name` is strictly safer and equals USD for the legacy rows.)
- Breakdown-month branch (Apr/May, growth still spills `realized − living`) and the annual `coerceAtMost` caps below are UNCHANGED. NO realizedPnL/DB/migration change. Build passed (`:app:compileDebugKotlin`).
Awaiting device verify (ANNUAL_TARGET log): Jan/Feb/Mar now log "LIVING-ONLY(combined)" with $2000/$2500/$3000 added to annualLivingFilled/Target; annual living bar grows to include them + Apr/May living; growth bar unchanged (~$5965 Apr/May spill).

### Group AO prime - COMPLETE (commit b641e96) — center journal sub-column header + content text
Device feedback on AM2: the right-header / left-content alignment looked less nice; user wants BOTH the headers AND the content lines CENTERED within each sub-column (columns themselves stay anchored — collected on the right half, realized on the left half).
- **File:** `PremiumIncomeScreen.kt` day sub-columns (~lines 666/674/698/711).
- **Change:** 4 `textAlign` swaps only — the two Hebrew headers ("פרמיות שנאספו:" line 666, "פרמיות שמומשו:" line 698) `TextAlign.Right`→`TextAlign.Center`; the two LTR-wrapped English ticker content lines (collected "${ticker} $strat ${amt}" line 674, realized "${ticker} $strat $closeLabel ${pnl}" line 711) `TextAlign.Left`→`TextAlign.Center`. `fillMaxWidth()` + the LTR `CompositionLocalProvider` wrap kept on all four.
- **Untouched:** two-column structure (collected = right `Box(weight 1f)`, realized = left `Box(weight 1f)`), the `סה"כ:` total sub-rows, everything else. NO realizedPnL/DB/migration change. Build passed (`:app:compileDebugKotlin`).
Awaiting device verify: each day's "פרמיות שנאספו"/"פרמיות שמומשו" header and the ticker lines under it are CENTERED within their column; collected column still on the right half, realized on the left half.

### Group AP prime - COMPLETE (commit abdb7aa) — NEW8 include stock holdings in portfolio-events ticker list so dividends show
Device logs confirmed Portfolio Events showed earnings for only the option-position tickers and ZERO dividends. ROOT CAUSE: `MainActivity.kt` (~line 648, `Screen.PortfolioEvents` composable) built `eventTickers = openPos.map{it.ticker}.distinct()` from open OPTION positions ONLY. Stock holdings with no open option (e.g. BTCI, a monthly dividend payer) were excluded, so the dividend/earnings fetch never ran for them. (The Yahoo v7 quote path in `PortfolioEventsScreen.kt` ~350-372 returning dividendDate/exDividendDate/dividendRate already works in the detail dialog — it just never ran for BTCI. Finnhub returns empty on free tier; dividend.com 403 — both dead but fail gracefully.)
- **Fix:** added `val evSummary by dashVm.dashboardSummary.collectAsState()` and changed `eventTickers` to `(openPos.map{it.ticker} + evSummary.stockPriceByTicker.keys).map{it.uppercase()}.distinct()`, plus `Log.w("DIV_CALL", "eventTickers (positions+holdings) = $eventTickers")`. Same approach as the AC2 social-filter fix (which added `summary.stockPriceByTicker` to the relevant set). `DashboardViewModel.dashboardSummary: StateFlow<DashboardSummary>` (line 1062); `DashboardSummary.stockPriceByTicker: Map<String,Double>` (DomainModels.kt:83, keys already uppercased from stockSnapshot).
- **Untouched:** PortfolioEventsScreen.kt sources (Finnhub/dividend.com left in, fail gracefully; Yahoo path relied on). NO realizedPnL/DB/migration change. Build passed (`:app:compileDebugKotlin`).
Awaiting device verify (DIV_CALL / DIV_PATH logs): eventTickers now lists all portfolio tickers incl. BTCI + other stock holdings (~14 not 7); BTCI shows a DIVIDEND event with ex/payment date; DIV_PATH "Fetched N events" shows dividends > 0.

### Group AQ prime - COMPLETE (commit f183e42) — NEW8 timing: re-fetch portfolio events when ticker set changes + cache per ticker-set
Device log after AP: DIV_CALL listed all 16 tickers (incl. BTCI) but the fetch still processed only the first 9 (option tickers) and logged "Fetched 7 events (0 dividends)". ROOT CAUSE: the fetch `LaunchedEffect` in `PortfolioEventsScreen.kt` (line 288) was keyed ONLY on `refreshTrigger`, so it ran ONCE at screen entry when `tickers` was still 9 — the dashboard summary carrying stock holdings hadn't emitted yet. When `tickers` grew to 16 the effect did NOT re-run, and the 6h cache (populated with the 9-ticker result) short-circuited any later run.
- **Fix (4 edits in PortfolioEventsScreen.kt):**
  1. `object EventsCache` (line ~54): added `var cachedTickerKey: String = ""`.
  2. Effect key (line 288): `LaunchedEffect(refreshTrigger)` → `LaunchedEffect(refreshTrigger, tickers)` — re-runs when the ticker list grows.
  3. Top of effect: `val tickerKey = tickers.map { it.uppercase() }.sorted().joinToString(",")` + `Log.w("DIV_CALL", "fetch effect: tickerKey=$tickerKey cachedKey=${EventsCache.cachedTickerKey}")`; cache gate (line 293) now also requires `EventsCache.cachedTickerKey == tickerKey`, so a cache built for the 9-set is invalid for the 16-set.
  4. On fresh fetch save (line ~704, beside `cachedEvents/lastFetchTime/cachedVersion`): `EventsCache.cachedTickerKey = tickerKey`.
- **Behavior:** when `tickers` grows from 9 → 16, the effect re-fires; `cachedTickerKey` (9-set) ≠ `tickerKey` (16-set) → fresh fetch over all 16. Empty-ticker early return unchanged (`resolvedTickers.isEmpty()`). Manual refresh path (sets cachedEvents=null) untouched. NO realizedPnL/DB/migration change. Build passed (`:app:compileDebugKotlin`).
Awaiting device verify: open Portfolio Events — DIV_CALL shows tickerKey changing 9-set→16-set and re-fetching; per-ticker DIV_PATH now includes BTCI/DRAM/NVDA/OPEN/etc.; "Fetched N events" shows dividends > 0 (BTCI monthly dividend appears).

### Group AR prime - COMPLETE (commit d025906) — NEW8 User-Agent on Yahoo dividend fetches
After AQ: all 16 tickers fetched and a dividend appeared for NVDA (mechanism works), but BTCI (monthly-distribution ETF) still returned historical=0 from every source. ROOT CAUSE: the Yahoo dividend fetches in `PortfolioEventsScreen.kt` used bare `java.net.URL(url).readText()` with NO User-Agent header — Yahoo serves empty/blocked bodies to non-browser clients. The working `IvService.fetchYahooPrice` uses `HttpURLConnection` + `setRequestProperty("User-Agent","Mozilla/5.0")` + 10s timeouts.
- **Fix:** added a top-level `private fun fetchUrlWithUA(urlStr: String): String` near the top of the file (above `object EventsCache`) — `HttpURLConnection`, `connectTimeout=readTimeout=10_000`, `User-Agent=Mozilla/5.0`, `inputStream.bufferedReader().readText()`, `disconnect()` in finally. Routed the THREE Yahoo fetches through it:
  - line 132: detail-dialog Yahoo v7 quote `val resp = fetchUrlWithUA(url)`
  - line 361: main Yahoo v7 quote `val yahooResp = fetchUrlWithUA(yahooUrl)`
  - line 583: Yahoo v8 chart dividend-history `val yResp = fetchUrlWithUA(yUrl)`
- **Untouched:** Finnhub fetches (earnings line 345, dividend2 future/hist 395/396, metric fallback 664) and dividend.com — left as bare readText (known/acceptable failures). Existing DIV_YAHOO "got N historical dividends" log kept. NO realizedPnL/DB/migration change. Build passed (`:app:compileDebugKotlin`).
Awaiting device verify: open Portfolio Events — DIV_YAHOO "got N historical dividends" shows N>0 for BTCI; projection emits upcoming monthly ex-dates; BTCI appears with DIVIDEND entries; "Fetched N events" shows >1 dividend.

### Group AS prime - COMPLETE (commit 4303968) — NEW8 dividend display: amount on card + quarterly projection + 25/30% tax
Device after AR: v8 history works (BTCI 8 pts, NVDA 2, MULL 1) but (a) v7 quote fails for all → no pay dates, (b) per-share amount not shown on card, (c) quarterly payers (NVDA, 2 pts) fell through to yield-only because projection needed interval 25-35d AND >=3 pts, (d) user wants Israeli dividend tax 25%/30% shown. 3 fixes in `PortfolioEventsScreen.kt`:
- **AS1 (projection ~line 641):** outer guard `parsedFuture.size <= 2 && parsedHist.size >= 2` (was >=3). Replaced `if (avgInterval in 25.0..35.0)` with `val isMonthly = avgInterval in 22.0..38.0; val isQuarterly = avgInterval in 80.0..100.0; if (isMonthly || isQuarterly) {...}`. Each projected `EarningsEvent` now sets `paymentDate = cursor.plusDays(7).toString()` (estimated, exDate+7) and keeps `dividendAmount = lastAmount`, `isProjected = true`. step=avgInterval.toLong(), up to 6 entries in 180d horizon (quarterly → ~1-2). DIV_PROJECT log `"$ticker avgInterval=$avgInterval monthly=$isMonthly quarterly=$isQuarterly projected=${projected.size}"`. Yield-only fallback (`if(!divFound)`) unchanged → only used when no usable history.
- **AS2 (list card ~line 844):** after the date/daysText Text, added `event.dividendAmount?.let { amt -> CompositionLocalProvider(Ltr){ Text("$${"%.4f".format(amt)}/מניה", labelSmall, PrimaryGreen, Bold) } }`.
- **AS3 (detail dialog ~line 210, inside existing `if (hasAmounts)`):** after "דיבידנד (תשלום זה)" added `run { val gross = totalPerPayment; Text("נטו אחרי מס 25%: ${formatCurrency(gross*0.75)}"); Text("נטו אחרי מס 30%: ${formatCurrency(gross*0.70)}"); Text("(30% כשיש מינוס במזומן/מרג'ין, 25% כשאין)", labelSmall, textTertiary) }` — both LTR-wrapped. Shows both rates so it's correct whether or not IBKR cash is negative.
- **Note:** dialog `var liveAmount by remember { mutableStateOf<Double?>(event.dividendAmount) }` (line 116) → v8-history amount flows through even though v7 fails (v7 only overwrites when liveAmount==null). NO realizedPnL/DB/migration change. Build passed (`:app:compileDebugKotlin`).
Awaiting device verify: each Ex-Dividend card shows "$X/מניה"; NVDA shows a projected next dividend with amount + estimated pay date (not yield-only); dividend dialog shows gross + net after 25% and 30% with the note.

## Verified fixed (device-confirmed)

N5 monthly bars + percent (J'/N'1 — pill shows 104%), N6 assignment prob (F'/G'), N7 collateral/maniot RTL (N'2/P1), N8 phantom tickers (F'), N9 collateral PUT-only (G'), N10 IV autofill removed (F'), N11 IV-opportunity hidden on edit (N'4), N12 feed color blue (F'), N13 social chronological + timestamps (P3), N14 debug notifications removed (N'3), "% on equity" (Q1), edit-screen ticker leak (P2/Q4), BS fill (I2), CSP card RTL (P1), ASTS live pre-market price (L'), **ASTS pre/post-market daily-change % (S0'1 — DEVICE-CONFIRMED -2.9% matches IBKR; was the long-running +4.85%/+20% bug)** — old N4 fully resolved.

## Awaiting device verification (time-dependent)

- **Z2 RESOLVED by AA2:** the DIAG log revealed the real cause was an accidental future-dated `2026-06:2000` target row (NOT the year_month string-format hypothesis). AA2 fixed it by capping the annual future-month lookup to the current month. year_month IS zero-padded ("2026-01") — format was never the issue. No further action needed unless more stray future-dated rows appear (then harden `getLatestTarget()` — see AA' call-site notes).
- **AB3 RESOLVED by AD1** (annual growth bar = 0): root cause was the annual `SegmentedTargetBar` doing living-FIRST on the 12× totals, so growth stayed 0 until YTD beat a full year of living. AD1 changed the ANNUAL bar to the SUM of per-month fills (each month's growth spill-over counts) via `SegmentedTargetBarSummed`. Awaiting device confirm that the annual growth bar now shows progress. (The `ANNUAL_TARGET "annual fills:"` log shows the summed values; the old "growth bar:" diagnostic in SegmentedTargetBar remains for the monthly bar.)
- The ASTS pre/post-market price + daily-change % items (old N4) are device-confirmed; see "Verified fixed". T-prime UI tweaks are code-verified via build but not yet device-confirmed by the user.

## NEW backlog (reported, not yet built)

NEW1. Feed "position edited": show what changed from→to (old value → new value).
NEW2. Abnormal alerts: +/- sign LEFT of number in "ירידה %"/"עלייה %".
NEW3. PLUG edit (price 4.07, call strike 2.50 = ITM) still shows "מחיר/עסקה מופלאה" — wrong BS calc, likely same IV-stale root as the delta -0.999 issue. Use IV_TRACE log (added in M2) to catch the bad IV value.
NEW4. Dashboard annual target: growth bar shows 0.
NEW5. Portfolio news: right-align AI summary + CACHE it (don't re-summarize on every open of the same article).
NEW6. Portfolio news: improve article content shown (lots of junk).
NEW7. Portfolio news: per-article summary with sentiment (pos/neg) + how the stock moved since the news (% up/down).
NEW8. Portfolio events: dividends not showing.
NEW9. Option to merge news + portfolio events into the social feed.
NEW10. (was N15) Alerts: show all highest IVs by current portfolio tickers + dates.

## Deferred (by user)

- Social: filter out irrelevant posts (e.g. Israeli market) — "future".
- Watchlist ±3% alert verification (needs device check, not code).

## In-progress feature plan (Group S prime — next)

S1: "Add to watchlist" button in add-position (always shown unless ticker already in watchlist); when added, ticker enters watchlist with all data and alerts on a target (e.g. price move ≥3%).
S2: Verify/ensure watchlist tickers trigger ±3% abnormal-move alerts.
S3: Available capital ("הון זמין") should auto-update from IBKR sync instead of manual entry in Settings.

## ✅ Confirmed working as of `8a44bd4`

This list is the source of truth — don't re-prompt items from here.

- Backup/Restore v4 (JSON, schema v4, autoFillCache + manualPnlAdjustment)
- Expected-profit calculator + calendar (state-aware: PLUG CC -$3367, SOFI -$3517, RIOT -$693, OWL -$17, IBRX +$254)
- Global left arrows (Unicode `←`, not Material Icons)
- Edit-position UX: tags removed, keyboard stays open during typing, premium recommendation
- DRAFT does not create activity-feed rows
- Portfolio sort persists across recompose (ViewModel + DataStore) + total shows full number
- Massive.com IV provider wired (Marketdata primary, Massive fallback) — but Massive fallback returns empty (Group D)
- Monthly realized — manual + IBKR combined
- Edit closed position (pre-fill + warning + "עדכן סגירה" button)
- Black-Scholes "מלא" works; premium NOT updated for CSP DRAFT (frozen), other strategies auto-update
- Save-as-draft → drafts list
- DB v30 + `initial_premium` column + migration with backfill (NOTE: `initial_iv` column does NOT yet exist — that's Group D)
- Top movers expand/collapse inline
- Portfolio events refresh (try/finally on isRefreshing)
- CC Reminder no longer says "CSP"
- CC Assignment Probability based on Black-Scholes delta
- IV per-contract trigger runs (but cache stays empty — that's Group D)
- Posts: LTR for English text in posts, POST_IMAGE callbacks run, new put starts at 0% (not 100%)

## Longer-term roadmap

The post-stabilization feature roadmap (F1–F16: AI per-post analysis, watchlist→AddPosition prefill, per-ticker news, pre-market mode, more social sources, home-screen widgets, paper trading, tax forms, spread redesign + drill-down, delete-confirm audit, transfer commissions, etc.) lives in `roadmap.md` — that file is the canonical source. The "NEW backlog" + "Group S prime" sections above are the actively-prioritized slice pulled from it.

## Validated environment notes

- **Pre-market ET:** 4:00–9:30 ET = **15:00–20:30 שעון תאילנד**
- **After-hours ET:** 16:00–20:00 ET = **03:00–07:00 שעון תאילנד**
- **Verified Marketdata-supported tickers (22):** IBRX, OPEN, BTCI, NVO, OWL, IREN, PLUG, RKLB, BBAI, EWY, ASTS, SOFI, RIOT, DRAM, NVDA, MULL, TTD, SMCI, ONDS, KEY, KBWY, USO, CHPT, LUNR, CCL
- **API keys present in DataStore:** Marketdata ✅, Massive ⚠️ (free tier returns empty IV), Finnhub, Alpha Vantage, Anthropic, Gemini, IBKR Flex token + Query ID. Alpaca **removed** (registration too complex).

## Workflow rules (reminders for all agents)

1. Max 4 fixes per Claude Code prompt
2. **GREP before fixing** — confirm grep target exists before writing changes
3. **Build must pass** before commit (`./gradlew :app:compileDebugKotlin --stacktrace`)
4. **Auto-accept all file edits** — Claude Code does not prompt for diff confirmation
5. **NEVER** change `realizedPnL()` without explicit Dima approval
6. Each fix has a verification step (specific log tag) that MUST appear in logcat before declaring done
7. Warn before any `uninstall` recommendation — past data loss

## Repo

- **GitHub:** funzi7/OptionsProfitTracker
- **Default branch:** `main`
- **Active branch:** `claude/analyze-project-structure-TC5ZG`
- **Branch naming convention:** `claude/batch{N}-{description}` or `claude/{round-label}-{description}`
- **PR convention:** conventional commits (`fix:`, `feat:`, `chore:`, etc.)
